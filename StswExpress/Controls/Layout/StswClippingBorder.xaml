<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:se="clr-namespace:StswExpress">

    <Style TargetType="{x:Type se:StswClippingBorder}">
        <Setter Property="BorderBrush" Value="{DynamicResource StswBorder.Static}"/>
        <Setter Property="BorderThickness" Value="2"/>
        
        <Setter Property="DoClipping" Value="True"/>

        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="VerticalAlignment" Value="Stretch"/>

        <Style.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="BorderBrush" Value="{DynamicResource StswBorder.Disabled}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>