<StackPanel x:Class="StswExpress.ColumnFilter"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
            xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
            xmlns:se="clr-namespace:StswExpress"
            mc:Ignorable="d" d:DesignWidth="100" Loaded="StackPanel_Loaded">
    <StackPanel.CommandBindings>
        <CommandBinding Command="se:Commands.Refresh" Executed="cmdRefresh_Executed"/>
    </StackPanel.CommandBindings>
    <TextBlock x:Name="txtHeader" Text="{Binding Header, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=StackPanel}}"/>
    <DockPanel Visibility="{Binding IsFilterVisible, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=StackPanel}, Converter={se:conv_Bool}}">
        <Image x:Name="imgMode" Height="{Binding iSize, Source={x:Static se:Settings.Default}}"
               Margin="0,0,2,0" PreviewMouseDown="imgMode_PreviewMouseDown">
            <Image.ContextMenu>
                <ContextMenu>
                    <ContextMenu.Resources>
                        <Style TargetType="{x:Type se:ExtMenuItem}">
                            <EventSetter Event="Click" Handler="miFilterMode_Click"/>
                        </Style>
                    </ContextMenu.Resources>
                    <se:ExtMenuItem Header="Jest równe" Tag="Equal" IconUri="/Resources/icon32_filter_equal.ico"/>
                    <se:ExtMenuItem Header="Nie jest równe" Tag="NotEqual" IconUri="/Resources/icon32_filter_notequal.ico"/>
                    <se:ExtMenuItem Header="Jest większe niż" Tag="Greater" IconUri="/Resources/icon32_filter_greater.ico"/>
                    <se:ExtMenuItem Header="Jest większe lub równe" Tag="GreaterEqual" IconUri="/Resources/icon32_filter_greaterequal.ico"/>
                    <se:ExtMenuItem Header="Jest mniejsze niż" Tag="Less" IconUri="/Resources/icon32_filter_less.ico"/>
                    <se:ExtMenuItem Header="Jest mniejsze lub równe" Tag="LessEqual" IconUri="/Resources/icon32_filter_lessequal.ico"/>
                    <se:ExtMenuItem Header="Pomiędzy" Tag="Between" IconUri="/Resources/icon32_filter_between.ico"/>
                    <se:ExtMenuItem Header="Zawiera" Tag="Contains" IconUri="/Resources/icon32_filter_contains.ico"/>
                    <se:ExtMenuItem Header="Nie zawiera" Tag="NotContains" IconUri="/Resources/icon32_filter_notcontains.ico"/>
                    <se:ExtMenuItem Header="Podobne do" Tag="Like" IconUri="/Resources/icon32_filter_like.ico"/>
                    <se:ExtMenuItem Header="Nie jest podobne" Tag="NotLike" IconUri="/Resources/icon32_filter_notlike.ico"/>
                    <se:ExtMenuItem Header="Rozpoczyna się od" Tag="StartsWith" IconUri="/Resources/icon32_filter_startswith.ico"/>
                    <se:ExtMenuItem Header="Kończy się na" Tag="EndsWith" IconUri="/Resources/icon32_filter_endswith.ico"/>
                    <se:ExtMenuItem Header="Zawiera listę" Tag="In" IconUri="/Resources/icon32_filter_in.ico"/>
                    <se:ExtMenuItem Header="Nie zawiera listy" Tag="NotIn" IconUri="/Resources/icon32_filter_notin.ico"/>
                </ContextMenu>
            </Image.ContextMenu>
        </Image>
        <UniformGrid x:Name="ugFilters" Columns="1"/>
    </DockPanel>
</StackPanel>
