<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:se="clr-namespace:StswExpress">

    <Style TargetType="{x:Type se:StswTimelineItem}">
        <Setter Property="Focusable" Value="False"/>
        <Setter Property="IsTabStop" Value="False"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>

        <Setter Property="Height" Value="20"/>
        <Setter Property="Width" Value="20"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type se:StswTimelineItem}">
                    <!-- main -->
                    <Grid x:Name="OPT_Root"
                          ToolTipService.InitialShowDelay="{TemplateBinding ToolTipInitialShowDelay}"
                          ToolTipService.ShowDuration="{TemplateBinding ToolTipShowDuration}">
                        <Grid.ToolTip>
                            <se:StswToolTip x:Name="OPT_ToolTip" Placement="Top">
                                <ContentPresenter Content="{TemplateBinding Content}"
                                                  ContentTemplate="{TemplateBinding PopupTemplate}"
                                                  ContentTemplateSelector="{TemplateBinding PopupTemplateSelector}"/>
                            </se:StswToolTip>
                        </Grid.ToolTip>
                        <!-- point -->
                        <Ellipse x:Name="OPT_Point"
                                 Fill="{TemplateBinding Foreground}"
                                 Stroke="{DynamicResource StswBorder.Static.Border}"
                                 StrokeThickness="1"/>
                    </Grid>
                    <!-- triggers -->
                    <ControlTemplate.Triggers>
                        <Trigger Property="Orientation" Value="Horizontal">
                            <Setter Property="Placement" Value="Top" TargetName="OPT_ToolTip"/>
                        </Trigger>
                        <Trigger Property="Orientation" Value="Vertical">
                            <Setter Property="Placement" Value="Right" TargetName="OPT_ToolTip"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>