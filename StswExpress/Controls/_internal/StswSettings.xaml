<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:sys="clr-namespace:System;assembly=mscorlib"
                    xmlns:se="clr-namespace:StswExpress">

    <Style TargetType="{x:Type se:StswSettings}">
        <Setter Property="Background" Value="{DynamicResource StswPanel.Static.Background}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource StswPanel.Static.Border}"/>
        
        <Setter Property="BorderThickness" Value="2"/>
        <Setter Property="CornerRadius" Value="10"/>
        <Setter Property="Padding" Value="3"/>
        
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="VerticalAlignment" Value="Stretch"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="VerticalContentAlignment" Value="Stretch"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type se:StswSettings}">
                    <se:StswGroupBox x:Name="PART_MainBorder"
                                     Background="{TemplateBinding Background}"
                                     BorderBrush="{TemplateBinding BorderBrush}"
                                     BorderThickness="{TemplateBinding BorderThickness}"
                                     CornerRadius="{TemplateBinding CornerRadius}"
                                     Focusable="False"
                                     Width="400"
                                     SnapsToDevicePixels="True"
                                     HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                     VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}">
                        <se:StswGroupBox.Header>
                            <StackPanel Height="20" Orientation="Horizontal" VerticalAlignment="Center">
                                <se:StswIcon Data="{x:Static se:StswIcons.Cog}" Fill="{DynamicResource StswColor.Designer}"/>
                                <se:StswText FontWeight="Medium"
                                             Padding="3 0 0 0"
                                             Text="{se:Tr StswSettings}"
                                             VerticalAlignment="Center"/>
                            </StackPanel>
                        </se:StswGroupBox.Header>
                        <DockPanel>
                            <!-- buttons -->
                            <UniformGrid DockPanel.Dock="Bottom" Rows="1" HorizontalAlignment="Center" Margin="{TemplateBinding Padding}">
                                <se:StswButton x:Name="PART_ButtonConfirm"
                                               Command="{Binding CloseCommand, RelativeSource={RelativeSource TemplatedParent}}"
                                               Margin="{TemplateBinding Padding}"
                                               Padding="15 5">
                                    <se:StswButton.CommandParameter>
                                        <sys:Boolean>True</sys:Boolean>
                                    </se:StswButton.CommandParameter>
                                    <se:StswHeader Content="Potwierdź" IconData="{x:Static se:StswIcons.Check}" IconFill="{DynamicResource StswColor.Positive}"/>
                                </se:StswButton>
                                <se:StswButton x:Name="PART_ButtonCancel"
                                               Command="{Binding CloseCommand, RelativeSource={RelativeSource TemplatedParent}}"
                                               CommandParameter="{x:Null}"
                                               Margin="{TemplateBinding Padding}"
                                               Padding="15 5">
                                    <se:StswHeader Content="Anuluj" IconData="{x:Static se:StswIcons.Cancel}" IconFill="{DynamicResource StswColor.Negative}"/>
                                </se:StswButton>
                            </UniformGrid>
                            <!-- isize -->
                            <StackPanel DockPanel.Dock="Top" Margin="0 0 0 10">
                                <se:StswHeader Content="Skalowanie:" IconData="{x:Static se:StswIcons.Scale}"/>
                                <DockPanel>
                                    <se:StswText Width="40" VerticalAlignment="Center">
                                        <Run Text="{Binding iSize, Source={x:Static se:Settings.Default}, StringFormat={}{0:N0}, Converter={x:Static se:StswMultiplyConverter.Instance}, ConverterParameter=100}"/>
                                        <Run Text="%"/>
                                    </se:StswText>
                                    <se:StswSlider Minimum="0.5" Maximum="3" TickFrequency="0.1" IsSnapToTickEnabled="True" Value="{Binding iSize, Source={x:Static se:Settings.Default}, UpdateSourceTrigger=PropertyChanged}"/>
                                </DockPanel>
                            </StackPanel>
                            <!-- theme -->
                            <StackPanel DockPanel.Dock="Top" Margin="0 0 0 10">
                                <se:StswHeader Content="Motyw:" IconData="{x:Static se:StswIcons.ThemeLightDark}"/>
                                <se:StswComboBox HorizontalContentAlignment="Left" SelectedValue="{Binding Theme, Source={x:Static se:Settings.Default}}" SelectedValuePath="Tag">
                                    <ComboBoxItem Tag="-1">
                                        <se:StswHeader Content="Automatyczny" IconData="{x:Static se:StswIcons.Cogs}" Padding="0"/>
                                    </ComboBoxItem>
                                    <ComboBoxItem Tag="0">
                                        <se:StswHeader Content="Jasny" IconData="{x:Static se:StswIcons.WeatherSunny}" Padding="0"/>
                                    </ComboBoxItem>
                                    <ComboBoxItem Tag="1">
                                        <se:StswHeader Content="Ciemny" IconData="{x:Static se:StswIcons.WeatherNight}" Padding="0"/>
                                    </ComboBoxItem>
                                    <ComboBoxItem Tag="2">
                                        <se:StswHeader Content="Różowy" IconData="{x:Static se:StswIcons.HeartOutline}" Padding="0"/>
                                    </ComboBoxItem>
                                </se:StswComboBox>
                            </StackPanel>
                            <!-- language -->
                            <StackPanel DockPanel.Dock="Top" Margin="0 0 0 10">
                                <se:StswHeader Content="Język:" IconData="{x:Static se:StswIcons.Translate}"/>
                                <se:StswComboBox HorizontalContentAlignment="Left" SelectedValue="{Binding Language, Source={x:Static se:Settings.Default}}" SelectedValuePath="Tag">
                                    <ComboBoxItem Tag="en">
                                        <se:StswHeader Content="{se:Tr StwsLanguageName, LanguageID=en}" Padding="0"/>
                                    </ComboBoxItem>
                                    <ComboBoxItem Tag="pl">
                                        <se:StswHeader Content="{se:Tr StwsLanguageName, LanguageID=pl}" Padding="0"/>
                                    </ComboBoxItem>
                                </se:StswComboBox>
                            </StackPanel>
                        </DockPanel>
                    </se:StswGroupBox>
                    <ControlTemplate.Triggers>
                        
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
</ResourceDictionary>