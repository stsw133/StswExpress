<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:se="clr-namespace:StswExpress">

    <Style TargetType="{x:Type se:StswNavigation}">

        <Setter Property="Background" Value="{DynamicResource StswBox.Static.Background}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource StswBox.Static.Border}"/>

        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="SubBorderThickness" Value="0.5"/>
        <Setter Property="CornerRadius" Value="0"/>

        <Setter Property="ExtendedMode" Value="True"/>
        <Setter Property="Orientation" Value="Vertical"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type se:StswNavigation}">
                    <se:ClippingBorder x:Name="PART_MainBorder"
                                       Background="{TemplateBinding Background}"
                                       BorderBrush="{TemplateBinding BorderBrush}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="{TemplateBinding CornerRadius}"
                                       SnapsToDevicePixels="True">
                        <DockPanel>
                            <!-- navigation list part -->
                            <DockPanel DockPanel.Dock="{TemplateBinding ItemsAlignment}">
                                <!-- function button -->
                                <se:StswToggleButton x:Name="PART_FunctionButton"
                                                     DockPanel.Dock="Top"
                                                     BorderThickness="0" CornerRadius="0" Focusable="False"
                                                     IsChecked="{Binding ExtendedMode, RelativeSource={RelativeSource TemplatedParent}}">
                                    <se:StswHeader x:Name="PART_FunctionButtonHeader" IconData="{x:Static se:StswIcons.Menu}" IconScale="1.5"/>
                                </se:StswToggleButton>
                                <!--
                                <DockPanel x:Name="PART_FunctionButton" DockPanel.Dock="Top">
                                    <se:StswSeparator x:Name="PART_SeparatorSpecial"
                                                      DockPanel.Dock="Top"
                                                      BorderBrush="{TemplateBinding BorderBrush}"
                                                      BorderThickness="{Binding SubBorderThickness.Left, RelativeSource={RelativeSource TemplatedParent}}"
                                                      Visibility="{TemplateBinding ExtendedMode, Converter={x:Static se:StswBoolConverter.Instance}}"/>
                                    <se:StswToggleButton BorderThickness="0" CornerRadius="0" IsChecked="{Binding ExtendedMode, RelativeSource={RelativeSource TemplatedParent}}">
                                        <se:StswHeader IconData="{x:Static se:StswIcons.Menu}" IconScale="1.5"/>
                                    </se:StswToggleButton>
                                </DockPanel>
                                -->
                                <se:StswSeparator x:Name="PART_SeparatorFirst"
                                                  DockPanel.Dock="Top"
                                                  BorderBrush="{TemplateBinding BorderBrush}"
                                                  BorderThickness="{Binding SubBorderThickness.Left, RelativeSource={RelativeSource TemplatedParent}}"/>
                                <!-- navigation items -->
                                <ItemsControl ItemsSource="{TemplateBinding Items}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <DockPanel/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.Resources>
                                        <Style TargetType="{x:Type se:StswNavigationElement}">
                                            <Setter Property="DockPanel.Dock" Value="Top"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ExtendedMode, RelativeSource={RelativeSource AncestorType={x:Type se:StswNavigation}}}" Value="False">
                                                    <Setter Property="IsCompact" Value="True"/>
                                                    <Setter Property="TextVisibility" Value="Collapsed"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Orientation, RelativeSource={RelativeSource AncestorType={x:Type se:StswNavigation}}}" Value="Horizontal">
                                                    <Setter Property="DockPanel.Dock" Value="Left"/>
                                                    <Setter Property="IsCompact" Value="True"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </ItemsControl.Resources>
                                </ItemsControl>
                            </DockPanel>
                            <!-- subborder -->
                            <Border x:Name="PART_SubBorder"
                                    DockPanel.Dock="{TemplateBinding ItemsAlignment}"
                                    BorderBrush="{Binding BorderBrush, ElementName=PART_MainBorder}"
                                    BorderThickness="{TemplateBinding SubBorderThickness}"
                                    SnapsToDevicePixels="True"/>
                            <!-- content -->
                            <ContentControl Content="{TemplateBinding Content}"
                                            ContentTemplate="{TemplateBinding ContentTemplate}"
                                            ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}"
                                            ContentStringFormat="{TemplateBinding ContentStringFormat}"/>
                        </DockPanel>
                    </se:ClippingBorder>
                    <ControlTemplate.Triggers>
                        <Trigger Property="ExtendedMode" Value="True">
                            <Setter Property="HorizontalAlignment" Value="Left" TargetName="PART_FunctionButton" />
                            <Setter Property="ContentVisibility" Value="Visible" TargetName="PART_FunctionButtonHeader" />
                        </Trigger>
                        <Trigger Property="Orientation" Value="Horizontal">
                            <Setter Property="DockPanel.Dock" Value="Top" TargetName="PART_MainBorder" />
                            <Setter Property="DockPanel.Dock" Value="Left" TargetName="PART_FunctionButton" />
                            <!--
                            <Setter Property="Visibility" Value="Collapsed" TargetName="PART_SeparatorSpecial" />
                            <Setter Property="DockPanel.Dock" Value="Left" TargetName="PART_SeparatorFirst" />
                            <Setter Property="DockPanel.Dock" Value="Left" TargetName="PART_SeparatorLast" />
                            -->
                            <!-- <Setter Property="DockPanel.Dock" Value="Top" TargetName="PART_SubBorder" /> -->
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
</ResourceDictionary>