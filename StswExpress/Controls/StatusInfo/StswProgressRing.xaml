<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:se="clr-namespace:StswExpress">

    <Style TargetType="{x:Type se:StswProgressRing}">
        <Setter Property="Background" Value="{DynamicResource StswProgressBar.Static.Background}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource StswProgressBar.Static.Border}"/>

        <Setter Property="BorderThickness" Value="1"/>
        <!-- <Setter Property="CornerRadius" Value="1"/> -->
        <Setter Property="Padding" Value="1"/>
<!--
        <Setter Property="State" Value="Running"/>
        <Setter Property="TextMode" Value="Percentage"/>
        -->
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="VerticalAlignment" Value="Stretch"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="VerticalContentAlignment" Value="Stretch"/>
        
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type se:StswProgressRing}">
                    <Viewbox>
                        <Grid>
                            <Ellipse RenderTransformOrigin="0.5 0.5"
                                     SnapsToDevicePixels="False"
                                     Stroke="Yellow"
                                     StrokeDashArray="0.628318 5"
                                     StrokeThickness="500" ToolTip="Hubert" ToolTipService.InitialShowDelay="0"
                                     Height="1000" Width="1000">
                                <Ellipse.RenderTransform>
                                    <RotateTransform Angle="-90"/>
                                </Ellipse.RenderTransform>
                            </Ellipse>
                            <Ellipse RenderTransformOrigin="0.5 0.5"
                                     SnapsToDevicePixels="False"
                                     Stroke="Blue"
                                     StrokeDashArray="1.884954 5"
                                     StrokeThickness="500" ToolTip="Grzegorz" ToolTipService.InitialShowDelay="0"
                                     Height="1000" Width="1000">
                                <Ellipse.RenderTransform>
                                    <RotateTransform Angle="-18"/>
                                </Ellipse.RenderTransform>
                            </Ellipse>
                            <Ellipse RenderTransformOrigin="0.5 0.5"
                                     SnapsToDevicePixels="False"
                                     Stroke="Red"
                                     StrokeDashArray="0.628318 5"
                                     StrokeThickness="500" ToolTip="Łukasz" ToolTipService.InitialShowDelay="0"
                                     Height="1000" Width="1000">
                                <Ellipse.RenderTransform>
                                    <RotateTransform Angle="198"/>
                                </Ellipse.RenderTransform>
                            </Ellipse>
                        </Grid>
                    </Viewbox>
                    <ControlTemplate.Triggers>
                        
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
</ResourceDictionary>