<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:se="clr-namespace:StswExpress">

    <Style TargetType="{x:Type se:StswComponentButton}">

        <Setter Property="Background" Value="#01777777"/>
        <Setter Property="Foreground" Value="{DynamicResource StswComponent.Static.Foreground}"/>

        <Setter Property="Padding" Value="3 1"/>
        
        <Setter Property="IconScale" Value="1.35"/>
        
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type se:StswComponentButton}">
                    <Border x:Name="PART_MainBorder" Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
                        <StackPanel Margin="1" Orientation="{TemplateBinding Orientation}">
                            <!-- icon -->
                            <ContentControl x:Name="PART_Icon"
                                            Focusable="False"
                                            Margin="{Binding ActualHeight, RelativeSource={RelativeSource Self}, Converter={x:Static se:StswMultiplyConverter.Instance}, ConverterParameter=0.1}"
                                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}">
                                <ContentControl.Style>
                                    <Style TargetType="{x:Type ContentControl}">
                                        <Style.Triggers>
                                            <!-- StswIcon -->
                                            <DataTrigger Binding="{Binding IsBusy, RelativeSource={RelativeSource TemplatedParent}}" Value="False">
                                                <Setter Property="ContentTemplate">
                                                    <Setter.Value>
                                                        <DataTemplate>
                                                            <se:StswIcon Data="{Binding IconData, RelativeSource={RelativeSource AncestorType={x:Type se:StswComponentButton}}}"
                                                                         Fill="{Binding Foreground, RelativeSource={RelativeSource Self}}">
                                                                <se:StswIcon.Style>
                                                                    <Style TargetType="{x:Type se:StswIcon}">
                                                                        <Style.Triggers>
                                                                            <DataTrigger Binding="{Binding IconScale, RelativeSource={RelativeSource AncestorType={x:Type se:StswComponentButton}}}" Value="{x:Null}">
                                                                                <Setter Property="Height" Value="{Binding ActualHeight, ElementName=PART_Text}"/>
                                                                                <Setter Property="Width" Value="{Binding ActualHeight, ElementName=PART_Text}"/>
                                                                            </DataTrigger>
                                                                            <DataTrigger Binding="{Binding IconScale, RelativeSource={RelativeSource AncestorType={x:Type se:StswComponentButton}}, Converter={x:Static se:StswNotNullConverter.Instance}}" Value="True">
                                                                                <Setter Property="Scale" Value="{Binding IconScale, RelativeSource={RelativeSource AncestorType={x:Type se:StswComponentButton}}}"/>
                                                                            </DataTrigger>
                                                                        </Style.Triggers>
                                                                    </Style>
                                                                </se:StswIcon.Style>
                                                            </se:StswIcon>
                                                        </DataTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </DataTrigger>
                                            <!-- StswLoadingCircle -->
                                            <DataTrigger Binding="{Binding IsBusy, RelativeSource={RelativeSource TemplatedParent}}" Value="True">
                                                <Setter Property="ContentTemplate">
                                                    <Setter.Value>
                                                        <DataTemplate>
                                                            <se:StswLoadingCircle Fill="{Binding Foreground, RelativeSource={RelativeSource Self}}">
                                                                <se:StswLoadingCircle.Style>
                                                                    <Style TargetType="{x:Type se:StswLoadingCircle}">
                                                                        <Style.Triggers>
                                                                            <DataTrigger Binding="{Binding IconScale, RelativeSource={RelativeSource AncestorType={x:Type se:StswComponentButton}}}" Value="{x:Null}">
                                                                                <Setter Property="Height" Value="{Binding ActualHeight, ElementName=PART_Text}"/>
                                                                                <Setter Property="Width" Value="{Binding ActualHeight, ElementName=PART_Text}"/>
                                                                            </DataTrigger>
                                                                            <DataTrigger Binding="{Binding IconScale, RelativeSource={RelativeSource AncestorType={x:Type se:StswComponentButton}}, Converter={x:Static se:StswNotNullConverter.Instance}}" Value="True">
                                                                                <Setter Property="Scale" Value="{Binding IconScale, RelativeSource={RelativeSource AncestorType={x:Type se:StswComponentButton}}}"/>
                                                                            </DataTrigger>
                                                                        </Style.Triggers>
                                                                    </Style>
                                                                </se:StswLoadingCircle.Style>
                                                            </se:StswLoadingCircle>
                                                        </DataTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </ContentControl.Style>
                            </ContentControl>
                            <!-- text -->
                            <ContentPresenter x:Name="PART_Text"
                                              Focusable="False"
                                              Margin="{TemplateBinding Padding}"
                                              RecognizesAccessKey="True"
                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              Visibility="{TemplateBinding ContentVisibility}"/>
                        </StackPanel>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="HasContent" Value="False">
                            <Setter Property="Visibility" Value="Collapsed" TargetName="PART_Text" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="TextElement.Foreground" Value="{DynamicResource StswComponent.MouseOver.Foreground}" TargetName="PART_MainBorder" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="TextElement.Foreground" Value="{DynamicResource StswComponent.Pressed.Foreground}" TargetName="PART_MainBorder" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Background" Value="Transparent" TargetName="PART_MainBorder" />
                            <Setter Property="TextElement.Foreground" Value="{DynamicResource StswComponent.Disabled.Foreground}" TargetName="PART_MainBorder" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
</ResourceDictionary>