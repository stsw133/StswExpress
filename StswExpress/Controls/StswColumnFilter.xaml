<UserControl x:Class="StswExpress.StswColumnFilter"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:se="clr-namespace:StswExpress"
             mc:Ignorable="d">

    <UserControl.Template>
        <ControlTemplate TargetType="{x:Type UserControl}">
            <DockPanel>
                <!-- filter part -->
                <Border DockPanel.Dock="Bottom"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{Binding CornerRadius, RelativeSource={RelativeSource TemplatedParent}}"
                        SnapsToDevicePixels="True"
                        Visibility="{Binding FilterVisibility, RelativeSource={RelativeSource TemplatedParent}}">
                    <DockPanel>
                        <!-- symbol -->
                        <se:StswButton x:Name="PART_Symbol"
                                       BorderThickness="0" CornerRadius="0" Padding="0"
                                       FontSize="{Binding iSize, Source={x:Static se:Settings.Default}, Converter={x:Static se:conv_Multiply.Instance}, ConverterParameter=1.25}"
                                       Width="{Binding iSize, Source={x:Static se:Settings.Default}, Converter={x:Static se:conv_Multiply.Instance}, ConverterParameter=2}"
                                       VerticalContentAlignment="Center"
                                       Click="PART_Symbol_Click">
                            <se:OutlinedTextBlock Stroke="Black" StrokeThickness="{Binding iSize, Source={x:Static se:Settings.Default}, Converter={x:Static se:conv_Multiply.Instance}, ConverterParameter=0.15}"/>
                            <se:StswButton.ContextMenu>
                                <ContextMenu>
                                    <ContextMenu.Resources>
                                        <Style TargetType="{x:Type MenuItem}">
                                            <EventSetter Event="Click" Handler="MnuItmFilterMode_Click"/>
                                        </Style>
                                        <Style TargetType="{x:Type se:OutlinedTextBlock}">
                                            <Setter Property="Stroke" Value="Black"/>
                                            <Setter Property="StrokeThickness" Value="2"/>
                                        </Style>
                                    </ContextMenu.Resources>

                                    <MenuItem Header="Jest równe" Tag="{x:Static se:StswColumnFilter+Modes.Equal}">
                                        <MenuItem.Icon>
                                            <se:OutlinedTextBlock Text="=" Fill="White"/>
                                        </MenuItem.Icon>
                                    </MenuItem>

                                    <MenuItem Header="Nie jest równe" Tag="{x:Static se:StswColumnFilter+Modes.NotEqual}">
                                        <MenuItem.Icon>
                                            <se:OutlinedTextBlock Text="≠" Fill="White"/>
                                        </MenuItem.Icon>
                                    </MenuItem>

                                    <MenuItem Header="Jest większe niż" Tag="{x:Static se:StswColumnFilter+Modes.Greater}">
                                        <MenuItem.Icon>
                                            <se:OutlinedTextBlock Text="&#62;" Fill="White"/>
                                        </MenuItem.Icon>
                                    </MenuItem>

                                    <MenuItem Header="Jest większe lub równe" Tag="{x:Static se:StswColumnFilter+Modes.GreaterEqual}">
                                        <MenuItem.Icon>
                                            <se:OutlinedTextBlock Text="≥" Fill="White"/>
                                        </MenuItem.Icon>
                                    </MenuItem>

                                    <MenuItem Header="Jest mniejsze niż" Tag="{x:Static se:StswColumnFilter+Modes.Less}">
                                        <MenuItem.Icon>
                                            <se:OutlinedTextBlock Text="&#60;" Fill="White"/>
                                        </MenuItem.Icon>
                                    </MenuItem>

                                    <MenuItem Header="Jest mniejsze lub równe" Tag="{x:Static se:StswColumnFilter+Modes.LessEqual}">
                                        <MenuItem.Icon>
                                            <se:OutlinedTextBlock Text="≤" Fill="White"/>
                                        </MenuItem.Icon>
                                    </MenuItem>

                                    <MenuItem Header="Pomiędzy" Tag="{x:Static se:StswColumnFilter+Modes.Between}">
                                        <MenuItem.Icon>
                                            <se:OutlinedTextBlock Text="≷" Fill="White"/>
                                        </MenuItem.Icon>
                                    </MenuItem>

                                    <MenuItem Header="Zawiera" Tag="{x:Static se:StswColumnFilter+Modes.Contains}">
                                        <MenuItem.Icon>
                                            <se:OutlinedTextBlock Text="⊇" Fill="#4D4"/>
                                        </MenuItem.Icon>
                                    </MenuItem>

                                    <MenuItem Header="Nie zawiera" Tag="{x:Static se:StswColumnFilter+Modes.NotContains}">
                                        <MenuItem.Icon>
                                            <se:OutlinedTextBlock Text="⊉" Fill="#D54"/>
                                        </MenuItem.Icon>
                                    </MenuItem>

                                    <MenuItem Header="Podobne do" Tag="{x:Static se:StswColumnFilter+Modes.Like}">
                                        <MenuItem.Icon>
                                            <se:OutlinedTextBlock Text="%" Fill="#4D4"/>
                                        </MenuItem.Icon>
                                    </MenuItem>

                                    <MenuItem Header="Nie jest podobne do" Tag="{x:Static se:StswColumnFilter+Modes.NotLike}">
                                        <MenuItem.Icon>
                                            <se:OutlinedTextBlock Text="%" Fill="#D54"/>
                                        </MenuItem.Icon>
                                    </MenuItem>

                                    <MenuItem Header="Rozpoczyna się od" Tag="{x:Static se:StswColumnFilter+Modes.StartsWith}">
                                        <MenuItem.Icon>
                                            <se:OutlinedTextBlock Text="⟝" Fill="#48D"/>
                                        </MenuItem.Icon>
                                    </MenuItem>

                                    <MenuItem Header="Kończy się na" Tag="{x:Static se:StswColumnFilter+Modes.EndsWith}">
                                        <MenuItem.Icon>
                                            <se:OutlinedTextBlock Text="⟞" Fill="#48D"/>
                                        </MenuItem.Icon>
                                    </MenuItem>

                                    <MenuItem Header="Zawiera listę" Tag="{x:Static se:StswColumnFilter+Modes.In}">
                                        <MenuItem.Icon>
                                            <se:OutlinedTextBlock Text="◐" Fill="#4D4"/>
                                        </MenuItem.Icon>
                                    </MenuItem>

                                    <MenuItem Header="Nie zawiera listy" Tag="{x:Static se:StswColumnFilter+Modes.NotIn}">
                                        <MenuItem.Icon>
                                            <se:OutlinedTextBlock Text="◐" Fill="#D54"/>
                                        </MenuItem.Icon>
                                    </MenuItem>

                                    <MenuItem Header="Puste" Tag="{x:Static se:StswColumnFilter+Modes.Null}">
                                        <MenuItem.Icon>
                                            <se:OutlinedTextBlock Text="○" Fill="White"/>
                                        </MenuItem.Icon>
                                    </MenuItem>

                                    <MenuItem Header="Niepuste" Tag="{x:Static se:StswColumnFilter+Modes.NotNull}">
                                        <MenuItem.Icon>
                                            <se:OutlinedTextBlock Text="⬤" Fill="#48D"/>
                                        </MenuItem.Icon>
                                    </MenuItem>

                                </ContextMenu>
                            </se:StswButton.ContextMenu>
                        </se:StswButton>
                        <Border x:Name="PART_SubBorder"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{Binding SubBorderThickness, RelativeSource={RelativeSource TemplatedParent}}"
                                SnapsToDevicePixels="True"/>
                        <UniformGrid x:Name="PART_Controls" Columns="1" Background="{DynamicResource TextBox.Static.Background}" PreviewKeyDown="PART_Controls_KeyDown"/>
                    </DockPanel>
                </Border>
                <!-- header part -->
                <DockPanel>
                    <!-- stats icon -->
                    <!--
                    <TextBlock DockPanel.Dock="Right" Text=" ✪"
                               ToolTip="{Binding StatsTooltip, RelativeSource={RelativeSource AncestorType=StackPanel}}"
                               Visibility="{Binding StatsVisibility, RelativeSource={RelativeSource AncestorType=StackPanel}}">
                        <TextBlock.Style>
                            <Style TargetType="{x:Type TextBlock}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding StatsForeground, RelativeSource={RelativeSource AncestorType=StackPanel}, Converter={x:Static se:conv_NotNull.Instance}}" Value="True">
                                        <Setter Property="Foreground" Value="{Binding StatsForeground, RelativeSource={RelativeSource AncestorType=StackPanel}}"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                    -->
                    <!-- name -->
                    <ContentControl Content="{Binding Header, RelativeSource={RelativeSource AncestorType={x:Type se:StswColumnFilter}}}"
                                    Margin="{Binding Padding, RelativeSource={RelativeSource AncestorType={x:Type se:StswColumnFilter}}}"/>
                </DockPanel>
            </DockPanel>
        </ControlTemplate>
    </UserControl.Template>
</UserControl>
