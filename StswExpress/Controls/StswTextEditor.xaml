<RichTextBox x:Class="StswExpress.StswTextEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:se="clr-namespace:StswExpress"
             mc:Ignorable="d" d:DesignHeight="200" d:DesignWidth="200">
    
    <RichTextBox.Template>
        <ControlTemplate TargetType="{x:Type RichTextBox}">
            <se:ClippingBorder x:Name="PART_MainBorder"
                               Background="{TemplateBinding Background}"
                               BorderBrush="{TemplateBinding BorderBrush}"
                               BorderThickness="{TemplateBinding BorderThickness}"
                               CornerRadius="{Binding CornerRadius, RelativeSource={RelativeSource TemplatedParent}}"
                               SnapsToDevicePixels="True">
                <DockPanel>
                    <Grid DockPanel.Dock="{Binding ButtonsAlignment, RelativeSource={RelativeSource TemplatedParent}}"
                          Background="#1777">
                        <!-- buttons -->
                        <StackPanel x:Name="PART_Button"
                                    Margin="{TemplateBinding Padding}"
                                    Orientation="Horizontal">
                            <se:StswButton CornerRadius="0" Margin="{TemplateBinding Padding}" Click="PART_ButtonLoad_Click">
                                <se:StswIcon Data="{x:Static se:StswIcons.FolderOpen}" Scale="1.5" Foreground="{x:Static se:StswColors.Structure}"/>
                            </se:StswButton>
                            <se:StswButton CornerRadius="0" Margin="{TemplateBinding Padding}" Click="PART_ButtonSave_Click">
                                <se:StswIcon Data="{x:Static se:StswIcons.FileExport}" Scale="1.5" Foreground="{x:Static se:StswColors.Positive}"/>
                            </se:StswButton>
                            <se:StswSeparator Margin="{TemplateBinding Padding}" Orientation="Vertical"/>
                            <se:StswToggleButton x:Name="PART_ButtonFormatBold" CornerRadius="0" Margin="{TemplateBinding Padding}" Click="PART_ButtonFormatBold_Click">
                                <se:StswIcon Data="{x:Static se:StswIcons.FormatBold}" Scale="1.5"/>
                            </se:StswToggleButton>
                            <se:StswToggleButton x:Name="PART_ButtonFormatItalic" CornerRadius="0" Margin="{TemplateBinding Padding}" Click="PART_ButtonFormatItalic_Click">
                                <se:StswIcon Data="{x:Static se:StswIcons.FormatItalic}" Scale="1.5"/>
                            </se:StswToggleButton>
                            <se:StswToggleButton x:Name="PART_ButtonFormatUnderline" CornerRadius="0" Margin="{TemplateBinding Padding}" Click="PART_ButtonFormatUnderline_Click">
                                <se:StswIcon Data="{x:Static se:StswIcons.FormatUnderline}" Scale="1.5"/>
                            </se:StswToggleButton>
                            <se:StswSeparator Margin="{TemplateBinding Padding}" Orientation="Vertical"/>
                            <se:StswComboBox x:Name="PART_FontFamilies" CornerRadius="0" Margin="{TemplateBinding Padding}" IsEditable="True"
                                             Width="{Binding iSize, Source={x:Static se:Settings.Default}, Converter={x:Static se:conv_Multiply.Conv}, ConverterParameter=15}"
                                             SelectionChanged="PART_FontFamilies_SelectionChanged"/>
                            <se:StswComboBox x:Name="PART_FontSizes" CornerRadius="0" Margin="{TemplateBinding Padding}" IsEditable="True"
                                             Width="{Binding iSize, Source={x:Static se:Settings.Default}, Converter={x:Static se:conv_Multiply.Conv}, ConverterParameter=5}"
                                             SelectionChanged="PART_FontSizes_SelectionChanged"/>
                        </StackPanel>
                    </Grid>
                    <!-- subborder -->
                    <Border x:Name="PART_SubBorder"
                            DockPanel.Dock="{Binding ButtonsAlignment, RelativeSource={RelativeSource TemplatedParent}}"
                            BorderBrush="{Binding BorderBrush, ElementName=PART_MainBorder}"
                            BorderThickness="{Binding SubBorderThickness, RelativeSource={RelativeSource TemplatedParent}}"
                            SnapsToDevicePixels="True"/>
                    <!-- custom buttons -->
                    <ItemsControl x:Name="PART_Buttons"
                                  DockPanel.Dock="{Binding ButtonsAlignment, RelativeSource={RelativeSource TemplatedParent}}"
                                  ItemsSource="{Binding Buttons, RelativeSource={RelativeSource TemplatedParent}}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                    </ItemsControl>
                    <!-- content -->
                    <ScrollViewer x:Name="PART_ContentHost" 
                                  Focusable="False" 
                                  HorizontalScrollBarVisibility="Hidden" 
                                  VerticalScrollBarVisibility="Hidden"/>
                </DockPanel>
            </se:ClippingBorder>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{Binding BackgroundMouseOver, RelativeSource={RelativeSource TemplatedParent}}" TargetName="PART_MainBorder" />
                    <Setter Property="BorderBrush" Value="{Binding BorderBrushMouseOver, RelativeSource={RelativeSource TemplatedParent}}" TargetName="PART_MainBorder" />
                    <Setter Property="TextElement.Foreground" Value="{Binding ForegroundMouseOver, RelativeSource={RelativeSource Self}}" />
                </Trigger>
                <Trigger Property="IsKeyboardFocused" Value="True">
                    <Setter Property="Background" Value="{Binding BackgroundFocused, RelativeSource={RelativeSource TemplatedParent}}" TargetName="PART_MainBorder" />
                    <Setter Property="BorderBrush" Value="{Binding BorderBrushFocused, RelativeSource={RelativeSource TemplatedParent}}" TargetName="PART_MainBorder" />
                    <Setter Property="TextElement.Foreground" Value="{Binding ForegroundFocused, RelativeSource={RelativeSource Self}}" />
                </Trigger>
                <Trigger Property="IsReadOnly" Value="True">
                    <Setter Property="Background" Value="{Binding BackgroundReadOnly, RelativeSource={RelativeSource TemplatedParent}}" TargetName="PART_MainBorder" />
                    <Setter Property="IsEnabled" Value="False" TargetName="PART_Button" />
                    <Setter Property="IsEnabled" Value="False" TargetName="PART_Buttons" />
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="{Binding BackgroundDisabled, RelativeSource={RelativeSource TemplatedParent}}" TargetName="PART_MainBorder" />
                    <Setter Property="BorderBrush" Value="{Binding BorderBrushDisabled, RelativeSource={RelativeSource TemplatedParent}}" TargetName="PART_MainBorder" />
                    <Setter Property="TextElement.Foreground" Value="{Binding ForegroundDisabled, RelativeSource={RelativeSource Self}}" />
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>
    </RichTextBox.Template>
</RichTextBox>
