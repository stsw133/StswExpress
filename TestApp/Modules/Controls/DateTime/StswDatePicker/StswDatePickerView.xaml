<UserControl x:Class="TestApp.StswDatePickerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:se="clr-namespace:StswExpress;assembly=StswExpress"
             xmlns:local="clr-namespace:TestApp"
             mc:Ignorable="d" d:Background="White" d:DesignHeight="450" d:DesignWidth="750">
    <d:UserControl.DataContext>
        <local:StswDatePickerContext/>
    </d:UserControl.DataContext>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <DockPanel>
            <Grid DockPanel.Dock="Top">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>
                <!-- name and description -->
                <StackPanel Grid.Column="0" Margin="5">
                    <Label DockPanel.Dock="Top">
                        <StackPanel>
                            <TextBlock Text="{Binding ThisControlName}" FontSize="20" FontWeight="Bold"/>
                            <TextBlock Margin="0 5 0 0" TextAlignment="Justify" TextWrapping="Wrap">
                                A control that allows users to select and display date.
                            </TextBlock>
                        </StackPanel>
                    </Label>
                    <!-- Components -->
                    <se:StswCheckBox IsChecked="{Binding Components}">
                        <Label DockPanel.Dock="Top" Content="Components" FontSize="15" FontWeight="Medium"/>
                    </se:StswCheckBox>
                    <!-- Format -->
                    <Label Content="Format:" FontSize="15" FontWeight="Medium"/>
                    <se:StswTextBox Text="{Binding Format}"/>
                    <!-- IncrementType -->
                    <Label Content="IncrementType:" FontSize="15" FontWeight="Medium"/>
                    <se:StswComboBox SelectedValue="{Binding IncrementType}" SelectedValuePath="Content">
                        <ComboBoxItem Content="{x:Static se:StswDateIncrementType.None}"/>
                        <ComboBoxItem Content="{x:Static se:StswDateIncrementType.Year}"/>
                        <ComboBoxItem Content="{x:Static se:StswDateIncrementType.Month}"/>
                        <ComboBoxItem Content="{x:Static se:StswDateIncrementType.Day}"/>
                        <ComboBoxItem Content="{x:Static se:StswDateIncrementType.Hour}"/>
                        <ComboBoxItem Content="{x:Static se:StswDateIncrementType.Minute}"/>
                        <ComboBoxItem Content="{x:Static se:StswDateIncrementType.Second}"/>
                    </se:StswComboBox>
                    <!-- IsEnabled -->
                    <se:StswCheckBox IsChecked="{Binding IsEnabled}">
                        <Label DockPanel.Dock="Top" Content="IsEnabled" FontSize="15" FontWeight="Medium"/>
                    </se:StswCheckBox>
                    <!-- IsReadOnly -->
                    <se:StswCheckBox IsChecked="{Binding IsReadOnly}">
                        <Label DockPanel.Dock="Top" Content="IsReadOnly" FontSize="15" FontWeight="Medium"/>
                    </se:StswCheckBox>
                    <!-- Minimum -->
                    <Label Content="Minimum:" FontSize="15" FontWeight="Medium"/>
                    <se:StswDatePicker Format="d" SelectedDate="{Binding Minimum}"/>
                    <!-- Maximum -->
                    <Label Content="Maximum:" FontSize="15" FontWeight="Medium"/>
                    <se:StswDatePicker Format="d" SelectedDate="{Binding Maximum}"/>
                </StackPanel>
                <GridSplitter Margin="-5" Width="5"/>
                <!-- control -->
                <DockPanel Grid.Column="1" Margin="5" VerticalAlignment="Top">
                    <se:StswDatePicker DockPanel.Dock="Top"
                                       Format="{Binding Format}"
                                       IncrementType="{Binding IncrementType}"
                                       IsEnabled="{Binding IsEnabled}"
                                       IsReadOnly="{Binding IsReadOnly}"
                                       Margin="5"
                                       Maximum="{Binding Maximum}"
                                       Minimum="{Binding Minimum}"
                                       Placeholder="Select date..."
                                       SelectedDate="{Binding SelectedDate}">
                        <se:StswDatePicker.Components>
                            <se:StswComponentButton IconData="{x:Static se:StswIcons.DiceMultiple}" Visibility="{Binding Components, Converter={x:Static se:StswBoolConverter.Instance}}" Command="{Binding RandomizeCommand}"/>
                        </se:StswDatePicker.Components>
                    </se:StswDatePicker>
                </DockPanel>
            </Grid>
        </DockPanel>
    </ScrollViewer>
</UserControl>
