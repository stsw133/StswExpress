<UserControl x:Class="TestApp.StswLoadingCircleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:se="clr-namespace:StswExpress;assembly=StswExpress"
             xmlns:local="clr-namespace:TestApp"
             mc:Ignorable="d" d:Background="White" d:DesignHeight="450" d:DesignWidth="750">
    <d:UserControl.DataContext>
        <local:StswLoadingCircleContext/>
    </d:UserControl.DataContext>

    <local:ControlsBase ContentAlignmentVisibility="Collapsed">
        <local:ControlsBase.Description>
            Represents a control displaying loading circle.
        </local:ControlsBase.Description>
        <!-- properties -->
        <local:ControlsBase.Properties>
            <!-- Scale -->
            <se:StswText FontSize="15" FontWeight="Medium" Padding="3" Text="Scale"/>
            <se:StswNumericBox Format="N2" Increment="0.1" Margin="0 0 0 3" Maximum="5" Minimum="0.1" Value="{Binding Scale}"/>
        </local:ControlsBase.Properties>
        <!-- content -->
        <se:StswLoadingCircle IsEnabled="{Binding IsEnabled}"
                              Margin="5"
                              Scale="{Binding Scale}"
                              HorizontalAlignment="{Binding HorizontalAlignment}"
                              VerticalAlignment="{Binding VerticalAlignment}"/>
        <!-- status panel -->

    </local:ControlsBase>
</UserControl>
