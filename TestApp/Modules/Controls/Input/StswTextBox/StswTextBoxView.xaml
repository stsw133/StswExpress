<UserControl x:Class="TestApp.StswTextBoxView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:se="clr-namespace:StswExpress;assembly=StswExpress"
             xmlns:local="clr-namespace:TestApp"
             mc:Ignorable="d" d:Background="White" d:DesignHeight="450" d:DesignWidth="750">
    <d:UserControl.DataContext>
        <local:StswTextBoxContext/>
    </d:UserControl.DataContext>
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <DockPanel VerticalAlignment="Top">
            <Label DockPanel.Dock="Top" Content="Disabled:"/>
            <UniformGrid DockPanel.Dock="Top" Rows="1">
                <se:StswTextBox Margin="5" IsEnabled="False" VerticalAlignment="Top" Text="{Binding Text, TargetNullValue=''}"/>
                <se:StswTextBox Margin="5" IsEnabled="False" VerticalAlignment="Top" Text="{Binding Text, TargetNullValue=''}">
                    <se:StswTextBox.Components>
                        <se:StswButton Padding="0" Command="{Binding SearchCommand}">
                            <se:StswButton.Resources>
                                <SolidColorBrush x:Key="StswButton.Static.Background" Color="Transparent" />
                                <SolidColorBrush x:Key="StswButton.Static.Border" Color="Transparent" />
                                <SolidColorBrush x:Key="StswButton.MouseOver.Background" Color="Transparent" />
                                <SolidColorBrush x:Key="StswButton.MouseOver.Border" Color="Transparent" />
                                <SolidColorBrush x:Key="StswButton.Pressed.Background" Color="Transparent" />
                                <SolidColorBrush x:Key="StswButton.Pressed.Border" Color="Transparent" />
                                <SolidColorBrush x:Key="StswButton.Disabled.Background" Color="Transparent" />
                                <SolidColorBrush x:Key="StswButton.Disabled.Border" Color="Transparent" />
                            </se:StswButton.Resources>
                            <se:StswButton.Style>
                                <Style TargetType="{x:Type se:StswButton}">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Foreground" Value="#F777"/>
                                        </Trigger>
                                        <Trigger Property="Button.IsPressed" Value="True">
                                            <Setter Property="Foreground" Value="#B777"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </se:StswButton.Style>
                            <se:StswIcon Data="{x:Static se:StswIcons.Magnify}" Scale="1.5"/>
                        </se:StswButton>
                    </se:StswTextBox.Components>
                </se:StswTextBox>
                <se:StswTextBox Margin="5" IsEnabled="False" VerticalContentAlignment="Top" Text="{Binding Text, TargetNullValue=''}" AcceptsReturn="True" TextWrapping="Wrap" Height="100" VerticalScrollBarVisibility="Auto">
                    <se:StswTextBox.Components>
                        <se:StswButton BorderThickness="2 0 0 0" CornerRadius="0" Command="{Binding ClearCommand}">
                            <se:StswIcon Data="{x:Static se:StswIcons.CloseCircleOutline}" Scale="1.5"/>
                        </se:StswButton>
                    </se:StswTextBox.Components>
                </se:StswTextBox>
            </UniformGrid>
            <Label DockPanel.Dock="Top" Content="Read only:"/>
            <UniformGrid DockPanel.Dock="Top" Rows="1">
                <se:StswTextBox Margin="5" IsReadOnly="True" VerticalAlignment="Top" Text="{Binding Text, TargetNullValue=''}"/>
                <se:StswTextBox Margin="5" IsReadOnly="True" VerticalAlignment="Top" Text="{Binding Text, TargetNullValue=''}">
                    <se:StswTextBox.Components>
                        <se:StswButton Padding="0" Command="{Binding SearchCommand}">
                            <se:StswButton.Resources>
                                <SolidColorBrush x:Key="StswButton.Static.Background" Color="Transparent" />
                                <SolidColorBrush x:Key="StswButton.Static.Border" Color="Transparent" />
                                <SolidColorBrush x:Key="StswButton.MouseOver.Background" Color="Transparent" />
                                <SolidColorBrush x:Key="StswButton.MouseOver.Border" Color="Transparent" />
                                <SolidColorBrush x:Key="StswButton.Pressed.Background" Color="Transparent" />
                                <SolidColorBrush x:Key="StswButton.Pressed.Border" Color="Transparent" />
                                <SolidColorBrush x:Key="StswButton.Disabled.Background" Color="Transparent" />
                                <SolidColorBrush x:Key="StswButton.Disabled.Border" Color="Transparent" />
                            </se:StswButton.Resources>
                            <se:StswButton.Style>
                                <Style TargetType="{x:Type se:StswButton}">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Foreground" Value="#F777"/>
                                        </Trigger>
                                        <Trigger Property="Button.IsPressed" Value="True">
                                            <Setter Property="Foreground" Value="#B777"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </se:StswButton.Style>
                            <se:StswIcon Data="{x:Static se:StswIcons.Magnify}" Scale="1.5"/>
                        </se:StswButton>
                    </se:StswTextBox.Components>
                </se:StswTextBox>
                <se:StswTextBox Margin="5" IsReadOnly="True" VerticalContentAlignment="Top" Text="{Binding Text, TargetNullValue=''}" AcceptsReturn="True" TextWrapping="Wrap" Height="100" VerticalScrollBarVisibility="Auto">
                    <se:StswTextBox.Components>
                        <se:StswButton BorderThickness="2 0 0 0" CornerRadius="0" IsHitTestVisible="False" Command="{Binding ClearCommand}">
                            <se:StswIcon Data="{x:Static se:StswIcons.CloseCircleOutline}" Scale="1.5"/>
                        </se:StswButton>
                    </se:StswTextBox.Components>
                </se:StswTextBox>
            </UniformGrid>
            <Label DockPanel.Dock="Top" Content="Enabled:"/>
            <UniformGrid DockPanel.Dock="Top" Rows="1">
                <se:StswTextBox Margin="5" VerticalAlignment="Top" Text="{Binding Text, TargetNullValue=''}" Placeholder="Wpisz coś..."/>
                <se:StswTextBox Margin="5" VerticalAlignment="Top" Text="{Binding Text, TargetNullValue=''}">
                    <se:StswTextBox.Components>
                        <se:StswButton Padding="0" Command="{Binding SearchCommand}">
                            <se:StswButton.Resources>
                                <SolidColorBrush x:Key="StswButton.Static.Background" Color="Transparent" />
                                <SolidColorBrush x:Key="StswButton.Static.Border" Color="Transparent" />
                                <SolidColorBrush x:Key="StswButton.MouseOver.Background" Color="Transparent" />
                                <SolidColorBrush x:Key="StswButton.MouseOver.Border" Color="Transparent" />
                                <SolidColorBrush x:Key="StswButton.Pressed.Background" Color="Transparent" />
                                <SolidColorBrush x:Key="StswButton.Pressed.Border" Color="Transparent" />
                                <SolidColorBrush x:Key="StswButton.Disabled.Background" Color="Transparent" />
                                <SolidColorBrush x:Key="StswButton.Disabled.Border" Color="Transparent" />
                            </se:StswButton.Resources>
                            <se:StswButton.Style>
                                <Style TargetType="{x:Type se:StswButton}">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Foreground" Value="#F777"/>
                                        </Trigger>
                                        <Trigger Property="Button.IsPressed" Value="True">
                                            <Setter Property="Foreground" Value="#B777"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </se:StswButton.Style>
                            <se:StswIcon Data="{x:Static se:StswIcons.Magnify}" Scale="1.5"/>
                        </se:StswButton>
                    </se:StswTextBox.Components>
                </se:StswTextBox>
                <se:StswTextBox Margin="5" VerticalContentAlignment="Top" Text="{Binding Text, TargetNullValue=''}" AcceptsReturn="True" TextWrapping="Wrap" Height="100" VerticalScrollBarVisibility="Auto">
                    <se:StswTextBox.Components>
                        <se:StswButton BorderThickness="2 0 0 0" CornerRadius="0" Command="{Binding ClearCommand}">
                            <se:StswIcon Data="{x:Static se:StswIcons.CloseCircleOutline}" Scale="1.5"/>
                        </se:StswButton>
                    </se:StswTextBox.Components>
                </se:StswTextBox>
            </UniformGrid>
        </DockPanel>
    </ScrollViewer>
</UserControl>
