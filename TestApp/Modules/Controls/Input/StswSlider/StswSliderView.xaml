<UserControl x:Class="TestApp.StswSliderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:se="clr-namespace:StswExpress;assembly=StswExpress"
             xmlns:local="clr-namespace:TestApp"
             mc:Ignorable="d" d:Background="White" d:DesignHeight="450" d:DesignWidth="750">
    <d:UserControl.DataContext>
        <local:StswSliderContext/>
    </d:UserControl.DataContext>

    <DockPanel>
        <Grid DockPanel.Dock="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>
            <!-- name and description -->
            <se:RgsScrollViewer>
                <StackPanel Margin="5">
                    <Label>
                        <StackPanel>
                            <TextBlock Text="{Binding ThisControlName}" FontSize="20" FontWeight="Bold"/>
                            <TextBlock Margin="0 5 0 0" TextAlignment="Justify" TextWrapping="Wrap">
                                Represents a control that lets the user select from a range of values by moving a Thumb control along a track.
                            </TextBlock>
                        </StackPanel>
                    </Label>
                    <!-- IsEnabled -->
                    <se:StswCheckBox IsChecked="{Binding IsEnabled}" Margin="0 3">
                        <Label Content="IsEnabled" FontSize="15" FontWeight="Medium"/>
                    </se:StswCheckBox>
                    <!-- Minimum -->
                    <Label Content="Minimum:" FontSize="15" FontWeight="Medium" Margin="0 3 0 0"/>
                    <se:StswNumericBox Format="N2" Margin="0 0 0 3" Value="{Binding Minimum}"/>
                    <!-- Maximum -->
                    <Label Content="Maximum:" FontSize="15" FontWeight="Medium" Margin="0 3 0 0"/>
                    <se:StswNumericBox Format="N2" Margin="0 0 0 3" Value="{Binding Maximum}"/>
                    <!-- SelectionStart -->
                    <Label Content="SelectionStart:" FontSize="15" FontWeight="Medium" Margin="0 3 0 0"/>
                    <se:StswNumericBox Format="N2" Margin="0 0 0 3" Value="{Binding SelectionStart}"/>
                    <!-- SelectionEnd -->
                    <Label Content="SelectionEnd:" FontSize="15" FontWeight="Medium" Margin="0 3 0 0"/>
                    <se:StswNumericBox Format="N2" Margin="0 0 0 3" Value="{Binding SelectionEnd}"/>
                    <!-- TickFrequency -->
                    <Label Content="TickFrequency:" FontSize="15" FontWeight="Medium" Margin="0 3 0 0"/>
                    <se:StswNumericBox Format="N2" Margin="0 0 0 3" Value="{Binding TickFrequency}"/>
                </StackPanel>
            </se:RgsScrollViewer>
            <GridSplitter Margin="-5" Width="5"/>
            <!-- control -->
            <DockPanel Grid.Column="1" Margin="5" VerticalAlignment="Top">
                <se:StswSlider DockPanel.Dock="Top"
                               IsEnabled="{Binding IsEnabled}"
                               IsSelectionRangeEnabled="True"
                               IsSnapToTickEnabled="True"
                               Margin="5"
                               Maximum="{Binding Maximum}"
                               Minimum="{Binding Minimum}"
                               SelectionEnd="{Binding SelectionEnd}"
                               SelectionStart="{Binding SelectionStart}"
                               TickFrequency="{Binding TickFrequency}"
                               TickPlacement="Both"
                               Value="{Binding SelectedValue}"/>
                <TextBlock DockPanel.Dock="Top" Margin="10">
                    <Run Text="Current value of slider: "/>
                    <Run Text="{Binding SelectedValue, StringFormat=N2}" FontWeight="Bold"/>
                </TextBlock>
            </DockPanel>
        </Grid>
    </DockPanel>
</UserControl>
