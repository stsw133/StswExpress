<UserControl x:Class="TestApp.StswSliderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:se="clr-namespace:StswExpress;assembly=StswExpress"
             xmlns:local="clr-namespace:TestApp"
             mc:Ignorable="d" d:Background="White" d:DesignHeight="450" d:DesignWidth="750">
    <d:UserControl.DataContext>
        <local:StswSliderContext/>
    </d:UserControl.DataContext>
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <DockPanel>
            <Label DockPanel.Dock="Top" Content="Disabled:"/>
            <se:StswSlider DockPanel.Dock="Top" Margin="5" IsEnabled="False"/>
            <Label DockPanel.Dock="Top" Content="Enabled:"/>
            <se:StswSlider DockPanel.Dock="Top" Margin="5" Minimum="0" Maximum="100" TickFrequency="5" IsSnapToTickEnabled="True" TickPlacement="Both" IsSelectionRangeEnabled="True" SelectionStart="20" SelectionEnd="40"/>
            <se:StswSlider DockPanel.Dock="Top" Margin="5" Minimum="0" Maximum="100" TickFrequency="5" IsSnapToTickEnabled="True" TickPlacement="Both" IsSelectionRangeEnabled="True" SelectionStart="20" SelectionEnd="40" Orientation="Vertical"/>
        </DockPanel>
    </ScrollViewer>
</UserControl>
