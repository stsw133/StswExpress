<UserControl x:Class="TestApp.StswListBoxView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:se="clr-namespace:StswExpress;assembly=StswExpress"
             xmlns:local="clr-namespace:TestApp"
             mc:Ignorable="d" d:Background="White" d:DesignHeight="450" d:DesignWidth="750">
    <d:UserControl.DataContext>
        <local:StswListBoxContext/>
    </d:UserControl.DataContext>
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <DockPanel VerticalAlignment="Top">
            <Label DockPanel.Dock="Top" Content="Disabled:"/>
            <se:StswListBox DockPanel.Dock="Top" Margin="5" ItemsSource="{Binding ListTypes}" DisplayMemberPath="Display" SelectedValuePath="Value" IsEnabled="False"/>
            <se:StswListBox DockPanel.Dock="Top" Margin="5" ItemsSource="{Binding ListTypes}" DisplayMemberPath="Display" SelectedValuePath="Value" SelectedItemsBinding="{Binding SelectedTypes}" IsEnabled="False" SelectionMode="Multiple"/>
            <Label DockPanel.Dock="Top" Content="Enabled:"/>
            <se:StswListBox DockPanel.Dock="Top" Margin="5" ItemsSource="{Binding ListTypes}" DisplayMemberPath="Display" SelectedValuePath="Value"/>
            <se:StswListBox DockPanel.Dock="Top" Margin="5" ItemsSource="{Binding ListTypes}" DisplayMemberPath="Display" SelectedValuePath="Value" SelectedItemsBinding="{Binding SelectedTypes}" SelectionMode="Multiple"/>
        </DockPanel>
    </ScrollViewer>
</UserControl>
