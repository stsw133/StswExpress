<UserControl x:Class="TestApp.StswColorSelectorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:se="clr-namespace:StswExpress;assembly=StswExpress"
             xmlns:local="clr-namespace:TestApp"
             mc:Ignorable="d" d:Background="White" d:DesignHeight="450" d:DesignWidth="750">
    <d:UserControl.DataContext>
        <local:StswColorSelectorContext/>
    </d:UserControl.DataContext>
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <DockPanel VerticalAlignment="Top">
            <Label DockPanel.Dock="Top" Content="Disabled:"/>
            <UniformGrid DockPanel.Dock="Top" Rows="1" HorizontalAlignment="Left">
                <se:StswColorSelector Margin="5" SelectedColor="{Binding Color}" IsEnabled="False"/>
                <se:StswColorSelector Margin="5" SelectedColor="{Binding Color}" IsEnabled="False"/>
            </UniformGrid>
            <Label DockPanel.Dock="Top" Content="Enabled:"/>
            <UniformGrid DockPanel.Dock="Top" Rows="1" HorizontalAlignment="Left">
                <se:StswColorSelector Margin="5" SelectedColor="{Binding Color}"/>
                <se:StswColorSelector Margin="5" SelectedColor="{Binding Color}"/>
            </UniformGrid>
        </DockPanel>
    </ScrollViewer>
</UserControl>
