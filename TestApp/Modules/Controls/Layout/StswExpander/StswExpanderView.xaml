<UserControl x:Class="TestApp.StswExpanderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:se="clr-namespace:StswExpress;assembly=StswExpress"
             xmlns:local="clr-namespace:TestApp"
             mc:Ignorable="d" d:Background="White" d:DesignHeight="450" d:DesignWidth="750">
    <d:UserControl.DataContext>
        <local:StswExpanderContext/>
    </d:UserControl.DataContext>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <DockPanel>
            <Grid DockPanel.Dock="Top">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>
                <!-- name and description -->
                <StackPanel Grid.Column="0" Margin="5">
                    <Label DockPanel.Dock="Top">
                        <StackPanel>
                            <TextBlock Text="{Binding ThisControlName}" FontSize="20" FontWeight="Bold"/>
                            <TextBlock Margin="0 5 0 0" TextAlignment="Justify" TextWrapping="Wrap">
                                Represents a control that displays a header and allows the user to collapse or expand content.
                            </TextBlock>
                        </StackPanel>
                    </Label>
                    <!-- ExpandDirection -->
                    <Label Content="ExpandDirection:" FontSize="15" FontWeight="Medium"/>
                    <se:StswComboBox SelectedValue="{Binding ExpandDirection}" SelectedValuePath="Content">
                        <ComboBoxItem Content="{x:Static ExpandDirection.Down}"/>
                        <ComboBoxItem Content="{x:Static ExpandDirection.Left}"/>
                        <ComboBoxItem Content="{x:Static ExpandDirection.Right}"/>
                        <ComboBoxItem Content="{x:Static ExpandDirection.Up}"/>
                    </se:StswComboBox>
                    <!-- IsEnabled -->
                    <se:StswCheckBox IsChecked="{Binding IsEnabled}">
                        <Label DockPanel.Dock="Top" Content="IsEnabled" FontSize="15" FontWeight="Medium"/>
                    </se:StswCheckBox>
                </StackPanel>
                <GridSplitter Margin="-5" Width="5"/>
                <!-- control -->
                <DockPanel Grid.Column="1" Margin="5" VerticalAlignment="Top">
                    <se:StswExpander DockPanel.Dock="Top"
                                     ExpandDirection="{Binding ExpandDirection}"
                                     IsEnabled="{Binding IsEnabled}"
                                     Margin="5">
                        <se:StswExpander.Header>
                            <se:StswHeader Content="Some header..." IconData="{x:Static se:StswIcons.Cake}"/>
                        </se:StswExpander.Header>
                        <Label Content="Some content..."/>
                    </se:StswExpander>
                </DockPanel>
            </Grid>
        </DockPanel>
    </ScrollViewer>
</UserControl>
