<UserControl x:Class="TestApp.StswExpanderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:se="clr-namespace:StswExpress;assembly=StswExpress"
             xmlns:local="clr-namespace:TestApp"
             mc:Ignorable="d" d:Background="White" d:DesignHeight="450" d:DesignWidth="750">
    <d:UserControl.DataContext>
        <local:StswExpanderContext/>
    </d:UserControl.DataContext>

    <DockPanel>
        <Grid DockPanel.Dock="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>
            <!-- name and description -->
            <se:StswScrollViewer IsDynamic="True">
                <StackPanel Margin="5">
                    <Label>
                        <StackPanel>
                            <TextBlock Text="{Binding ThisControlName}" FontSize="20" FontWeight="Bold"/>
                            <TextBlock Margin="0 5 0 0" TextAlignment="Justify" TextWrapping="Wrap">
                                Represents a control that displays a header and allows the user to collapse or expand content.
                            </TextBlock>
                        </StackPanel>
                    </Label>
                    <!-- ExpandDirection -->
                    <Label Content="ExpandDirection:" FontSize="15" FontWeight="Medium" Margin="0 3 0 0"/>
                    <se:StswComboBox ItemsSource="{Binding Converter={x:Static se:StswEnumToListConverter.Instance}, ConverterParameter={x:Type ExpandDirection}}"
                                     DisplayMemberPath="Display" Margin="0 0 0 3" SelectedValue="{Binding ExpandDirection}" SelectedValuePath="Value"/>
                    <!-- HorizontalAlignment -->
                    <Label Content="HorizontalAlignment:" FontSize="15" FontWeight="Medium" Margin="0 3 0 0"/>
                    <se:StswComboBox ItemsSource="{Binding Converter={x:Static se:StswEnumToListConverter.Instance}, ConverterParameter={x:Type HorizontalAlignment}}"
                                     DisplayMemberPath="Display" Margin="0 0 0 3" SelectedValue="{Binding HorizontalAlignment}" SelectedValuePath="Value"/>
                    <!-- VerticalAlignment -->
                    <Label Content="VerticalAlignment:" FontSize="15" FontWeight="Medium" Margin="0 3 0 0"/>
                    <se:StswComboBox ItemsSource="{Binding Converter={x:Static se:StswEnumToListConverter.Instance}, ConverterParameter={x:Type VerticalAlignment}}"
                                     DisplayMemberPath="Display" Margin="0 0 0 3" SelectedValue="{Binding VerticalAlignment}" SelectedValuePath="Value"/>
                    <!-- IsEnabled -->
                    <se:StswCheckBox IsChecked="{Binding IsEnabled}" Margin="0 3">
                        <Label Content="IsEnabled" FontSize="15" FontWeight="Medium"/>
                    </se:StswCheckBox>
                </StackPanel>
            </se:StswScrollViewer>
            <GridSplitter Margin="-5" Width="5"/>
            <!-- control -->
            <DockPanel Grid.Column="1" Margin="5">
                <se:StswExpander DockPanel.Dock="Top"
                                 ExpandDirection="{Binding ExpandDirection}"
                                 IsEnabled="{Binding IsEnabled}"
                                 Margin="5"
                                 HorizontalAlignment="{Binding HorizontalAlignment}"
                                 VerticalAlignment="{Binding VerticalAlignment}">
                    <se:StswExpander.Header>
                        <se:StswHeader Content="Some header..." IconData="{x:Static se:StswIcons.Cake}"/>
                    </se:StswExpander.Header>
                    <Label Content="Some content..."/>
                </se:StswExpander>
            </DockPanel>
        </Grid>
    </DockPanel>
</UserControl>
