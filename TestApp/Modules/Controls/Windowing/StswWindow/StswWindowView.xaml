<UserControl x:Class="TestApp.StswWindowView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:se="clr-namespace:StswExpress;assembly=StswExpress"
             xmlns:local="clr-namespace:TestApp"
             mc:Ignorable="d" d:DataContext="{d:DesignInstance local:StswWindowContext}">

    <local:ControlsBase ContentAlignmentVisibility="Collapsed">
        <local:ControlsBase.Description>
            Represents a window control with additional functionality and customization options,
            such as full-screen toggle, default size management, and custom window chrome.
        </local:ControlsBase.Description>
        <!-- properties -->

        <!-- content -->
        <se:StswButton Command="{Binding OpenNewWindowCommand}" Content="Open new window"/>
        <!-- status panel -->
        
    </local:ControlsBase>
</UserControl>
