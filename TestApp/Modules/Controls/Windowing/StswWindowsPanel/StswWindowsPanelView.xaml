<UserControl x:Class="TestApp.StswWindowsPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TestApp"
             xmlns:se="clr-namespace:StswExpress;assembly=StswExpress"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <local:ControlsBase ContentAlignmentVisibility="Visible">
        <local:ControlsBase.Description>
            Represents a control that displays a collection of items as windows.
        </local:ControlsBase.Description>
        <!-- properties -->
        <!--<local:ControlsBase.Properties>-->
            <!-- SelectionMode -->
            <!--<se:StswText FontSize="15" FontWeight="Medium" Padding="3" Text="{se:StswNameOf SelectionMode, Type={x:Type se:StswListBox}}"/>
            <se:StswComboBox ItemsSource="{se:StswEnumToList SelectionMode}" Margin="0 0 0 3" SelectedValue="{Binding SelectionMode}"/>-->
        <!--</local:ControlsBase.Properties>-->
        <!-- content -->
        <se:StswWindowsPanel ItemsSource="{Binding Contexts}" Background="Green"
                             CloseCommand="{Binding CloseWindowCommand}">
        </se:StswWindowsPanel>
        <!-- status panel -->
        <!--<local:ControlsBase.StatusPanel>
            
        </local:ControlsBase.StatusPanel>-->
    </local:ControlsBase>
</UserControl>
