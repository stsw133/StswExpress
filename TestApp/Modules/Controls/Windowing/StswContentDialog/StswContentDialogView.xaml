<UserControl x:Class="TestApp.StswContentDialogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:se="clr-namespace:StswExpress;assembly=StswExpress"
             xmlns:local="clr-namespace:TestApp"
             mc:Ignorable="d" d:Background="White" d:DesignHeight="450" d:DesignWidth="750">
    <d:UserControl.DataContext>
        <local:StswContentDialogContext/>
    </d:UserControl.DataContext>

    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <DockPanel>
                <Grid DockPanel.Dock="Top">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="2*"/>
                    </Grid.ColumnDefinitions>
                    <!-- name and description -->
                    <StackPanel Grid.Column="0" Margin="5">
                        <Label DockPanel.Dock="Top">
                            <StackPanel>
                                <TextBlock Text="{Binding ThisControlName}" FontSize="20" FontWeight="Bold"/>
                                <TextBlock Margin="0 5 0 0" TextAlignment="Justify" TextWrapping="Wrap">
                                    Represents a control behaving like content dialog with various properties for customization.
                                </TextBlock>
                            </StackPanel>
                        </Label>
                        <!-- Buttons -->
                        <Label Content="Buttons:" FontSize="15" FontWeight="Medium"/>
                        <se:StswComboBox SelectedValue="{Binding ContentDialogButtons}" SelectedValuePath="Content">
                            <ComboBoxItem Content="{x:Static se:StswDialogButtons.OK}"/>
                            <ComboBoxItem Content="{x:Static se:StswDialogButtons.YesNo}"/>
                            <ComboBoxItem Content="{x:Static se:StswDialogButtons.OKCancel}"/>
                            <ComboBoxItem Content="{x:Static se:StswDialogButtons.YesNoCancel}"/>
                        </se:StswComboBox>
                        <!-- Image -->
                        <Label Content="Image:" FontSize="15" FontWeight="Medium"/>
                        <se:StswComboBox SelectedValue="{Binding ContentDialogImage}" SelectedValuePath="Content">
                            <ComboBoxItem Content="{x:Static se:StswDialogImage.None}"/>
                            <ComboBoxItem Content="{x:Static se:StswDialogImage.Error}"/>
                            <ComboBoxItem Content="{x:Static se:StswDialogImage.Information}"/>
                            <ComboBoxItem Content="{x:Static se:StswDialogImage.Question}"/>
                            <ComboBoxItem Content="{x:Static se:StswDialogImage.Warning}"/>
                        </se:StswComboBox>
                        <!-- IsEnabled -->
                        <!--
                        <se:StswCheckBox IsChecked="{Binding IsEnabled}">
                            <Label DockPanel.Dock="Top" Content="IsEnabled" FontSize="15" FontWeight="Medium"/>
                        </se:StswCheckBox>
                        -->
                    </StackPanel>
                    <GridSplitter Margin="-5" Width="5"/>
                    <!-- control -->
                    <DockPanel Grid.Column="1" Margin="5" VerticalAlignment="Top">
                        <se:StswButton DockPanel.Dock="Top"
                                       Command="{Binding OpenContentDialogCommand}"
                                       Content="Open content dialog"
                                       Margin="5"/>
                        <TextBlock DockPanel.Dock="Top" Margin="10">
                            <Run Text="Dialog ended with "/>
                            <Run Text="{Binding ContentDialogResult}" FontWeight="Bold"/>
                            <Run Text=" result"/>
                        </TextBlock>
                    </DockPanel>
                </Grid>
            </DockPanel>
        </ScrollViewer>
        <se:StswContentDialog BindingModel="{Binding ContentDialogBinding}"/>
    </Grid>
</UserControl>
