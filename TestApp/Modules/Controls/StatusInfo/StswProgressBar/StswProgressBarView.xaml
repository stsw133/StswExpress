<UserControl x:Class="TestApp.StswProgressBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:se="clr-namespace:StswExpress;assembly=StswExpress"
             xmlns:local="clr-namespace:TestApp"
             mc:Ignorable="d" d:Background="White" d:DesignHeight="450" d:DesignWidth="750">
    <d:UserControl.DataContext>
        <local:StswProgressBarContext/>
    </d:UserControl.DataContext>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <DockPanel>
            <Grid DockPanel.Dock="Top">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>
                <!-- name and description -->
                <StackPanel Grid.Column="0" Margin="5">
                    <Label DockPanel.Dock="Top">
                        <StackPanel>
                            <TextBlock Text="{Binding ThisControlName}" FontSize="20" FontWeight="Bold"/>
                            <TextBlock Margin="0 5 0 0" TextAlignment="Justify" TextWrapping="Wrap">
                                Represents a control being a progress bar with additional features such as displaying progress as text and different states.
                            </TextBlock>
                        </StackPanel>
                    </Label>
                    <!-- IsEnabled -->
                    <se:StswCheckBox IsChecked="{Binding IsEnabled}">
                        <Label DockPanel.Dock="Top" Content="IsEnabled" FontSize="15" FontWeight="Medium"/>
                    </se:StswCheckBox>
                    <!-- IsIndeterminate -->
                    <se:StswCheckBox IsChecked="{Binding IsIndeterminate}">
                        <Label DockPanel.Dock="Top" Content="IsIndeterminate" FontSize="15" FontWeight="Medium"/>
                    </se:StswCheckBox>
                    <!-- Minimum -->
                    <Label Content="Minimum:" FontSize="15" FontWeight="Medium"/>
                    <se:StswNumericBox Format="N2" Value="{Binding Minimum}"/>
                    <!-- Maximum -->
                    <Label Content="Maximum:" FontSize="15" FontWeight="Medium"/>
                    <se:StswNumericBox Format="N2" Value="{Binding Maximum}"/>
                    <!-- State -->
                    <Label Content="State:" FontSize="15" FontWeight="Medium"/>
                    <se:StswComboBox SelectedValue="{Binding State}" SelectedValuePath="Content">
                        <ComboBoxItem Content="{x:Static se:StswProgressBar+States.Ready}"/>
                        <ComboBoxItem Content="{x:Static se:StswProgressBar+States.Running}"/>
                        <ComboBoxItem Content="{x:Static se:StswProgressBar+States.Paused}"/>
                        <ComboBoxItem Content="{x:Static se:StswProgressBar+States.Error}"/>
                    </se:StswComboBox>
                    <!-- TextInPercents -->
                    <se:StswCheckBox IsChecked="{Binding TextInPercents}" IsThreeState="True">
                        <Label DockPanel.Dock="Top" Content="TextInPercents" FontSize="15" FontWeight="Medium"/>
                    </se:StswCheckBox>
                    <!-- Value -->
                    <Label Content="Value:" FontSize="15" FontWeight="Medium"/>
                    <se:StswNumericBox Format="N2" Value="{Binding SelectedValue}"/>
                </StackPanel>
                <GridSplitter Margin="-5" Width="5"/>
                <!-- control -->
                <DockPanel Grid.Column="1" Margin="5" VerticalAlignment="Top">
                    <se:StswProgressBar DockPanel.Dock="Top"
                                        IsEnabled="{Binding IsEnabled}"
                                        IsIndeterminate="{Binding IsIndeterminate}"
                                        Margin="5"
                                        Maximum="{Binding Maximum}"
                                        Minimum="{Binding Minimum}"
                                        State="{Binding State}"
                                        TextInPercents="{Binding TextInPercents}"
                                        Value="{Binding SelectedValue}"/>
                </DockPanel>
            </Grid>
        </DockPanel>
    </ScrollViewer>
</UserControl>
