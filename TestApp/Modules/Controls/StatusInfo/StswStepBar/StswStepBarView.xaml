<UserControl x:Class="TestApp.StswStepBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:se="clr-namespace:StswExpress;assembly=StswExpress"
             xmlns:local="clr-namespace:TestApp"
             mc:Ignorable="d" d:DataContext="{d:DesignInstance local:StswStepBarContext}">

    <local:ControlsBase ContentAlignmentVisibility="Collapsed">
        <local:ControlsBase.Description>
            Represents a control that displays a sequence of steps to indicate progress through a process.
        </local:ControlsBase.Description>
        <!-- properties
        <local:ControlsBase.Properties>
        
        </local:ControlsBase.Properties> -->
        <!-- content -->
        <se:StswStepBar IsEnabled="{Binding IsEnabled}"
                        StepNumber="{Binding StepNumber}"
                        HorizontalAlignment="{Binding HorizontalAlignment}"
                        HorizontalContentAlignment="{Binding HorizontalContentAlignment}"
                        VerticalAlignment="{Binding VerticalAlignment}"
                        VerticalContentAlignment="{Binding VerticalContentAlignment}">
            <se:StswStepBar.Steps>
                <se:StswStepBarItem/>
                <se:StswStepBarItem/>
                <se:StswStepBarItem/>
                <se:StswStepBarItem/>
                <se:StswStepBarItem/>
                <se:StswStepBarItem/>
            </se:StswStepBar.Steps>
        </se:StswStepBar>
        <!-- status panel -->
        <local:ControlsBase.StatusPanel>
            <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
                <se:StswSubButton Command="{Binding PreviousStepCommand}" IconData="{x:Static se:StswIcons.ArrowLeftBold}"/>
                <se:StswTextBox IsReadOnly="True" Text="{Binding StepNumber}"/>
                <se:StswSubButton Command="{Binding NextStepCommand}" IconData="{x:Static se:StswIcons.ArrowRightBold}"/>
            </StackPanel>
        </local:ControlsBase.StatusPanel>
    </local:ControlsBase>
</UserControl>
