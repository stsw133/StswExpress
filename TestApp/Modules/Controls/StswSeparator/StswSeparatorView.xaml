<UserControl x:Class="TestApp.StswSeparatorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:se="clr-namespace:StswExpress;assembly=StswExpress"
             xmlns:local="clr-namespace:TestApp"
             mc:Ignorable="d" d:Background="White" d:DesignHeight="450" d:DesignWidth="750">
    <d:UserControl.DataContext>
        <local:StswSeparatorContext/>
    </d:UserControl.DataContext>
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <DockPanel VerticalAlignment="Stretch">
            <Label DockPanel.Dock="Top" Content="Horizontal:"/>
            <se:StswSeparator DockPanel.Dock="Top" Margin="5" Orientation="Horizontal"/>
            <Label DockPanel.Dock="Top" Content="Vertical:"/>
            <se:StswSeparator DockPanel.Dock="Top" Margin="5" Orientation="Vertical"/>
        </DockPanel>
    </ScrollViewer>
</UserControl>
