<UserControl x:Class="TestApp.StswRadioButtonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:se="clr-namespace:StswExpress;assembly=StswExpress"
             xmlns:local="clr-namespace:TestApp"
             mc:Ignorable="d" d:Background="White" d:DesignHeight="450" d:DesignWidth="750">
    <d:UserControl.DataContext>
        <local:StswRadioButtonContext/>
    </d:UserControl.DataContext>
    
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <DockPanel>
            <Grid DockPanel.Dock="Top">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>
                <!-- name and description -->
                <StackPanel Grid.Column="0" Margin="5">
                    <Label DockPanel.Dock="Top">
                        <StackPanel>
                            <TextBlock Text="{Binding ThisControlName}" FontSize="20" FontWeight="Bold"/>
                            <TextBlock Margin="0 5 0 0" TextAlignment="Justify" TextWrapping="Wrap">
                                Represents a control that allows the user to select a single option from a group of mutually exclusive options.
                            </TextBlock>
                        </StackPanel>
                    </Label>
                    <!-- IsEnabled -->
                    <se:StswCheckBox IsChecked="{Binding IsEnabled}">
                        <Label DockPanel.Dock="Top" Content="IsEnabled" FontSize="15" FontWeight="Medium"/>
                    </se:StswCheckBox>
                </StackPanel>
                <GridSplitter Margin="-5" Width="5"/>
                <!-- control -->
                <DockPanel Grid.Column="1" Margin="5" VerticalAlignment="Top">
                    <UniformGrid DockPanel.Dock="Top" Columns="1" Margin="5">
                        <se:StswRadioButton IsEnabled="{Binding IsEnabled}" Margin="0 0 0 5" Command="{Binding OnClickCommand}" CommandParameter="1" IsChecked="{Binding SelectedOption[0]}">
                            <se:StswHeader IconData="{x:Static se:StswIcons.Dice1}" Content="Option 1"/>
                        </se:StswRadioButton>
                        <se:StswRadioButton IsEnabled="{Binding IsEnabled}" Margin="0 0 0 5" Command="{Binding OnClickCommand}" CommandParameter="2" IsChecked="{Binding SelectedOption[1]}">
                            <se:StswHeader IconData="{x:Static se:StswIcons.Dice2}" Content="Option 2"/>
                        </se:StswRadioButton>
                        <se:StswRadioButton IsEnabled="{Binding IsEnabled}" Margin="0 0 0 5" Command="{Binding OnClickCommand}" CommandParameter="3" IsChecked="{Binding SelectedOption[2]}">
                            <se:StswHeader IconData="{x:Static se:StswIcons.Dice3}" Content="Option 3"/>
                        </se:StswRadioButton>
                        <se:StswRadioButton IsEnabled="{Binding IsEnabled}" Margin="0 0 0 5" Command="{Binding OnClickCommand}" CommandParameter="4" IsChecked="{Binding SelectedOption[3]}">
                            <se:StswHeader IconData="{x:Static se:StswIcons.Dice4}" Content="Option 4"/>
                        </se:StswRadioButton>
                        <se:StswRadioButton IsEnabled="{Binding IsEnabled}" Margin="0 0 0 5" Command="{Binding OnClickCommand}" CommandParameter="5" IsChecked="{Binding SelectedOption[4]}">
                            <se:StswHeader IconData="{x:Static se:StswIcons.Dice5}" Content="Option 5"/>
                        </se:StswRadioButton>
                    </UniformGrid>
                    <TextBlock DockPanel.Dock="Top" Margin="10">
                        <Run Text="You selected option "/>
                        <Run Text="{Binding ClickOption}" FontWeight="Bold"/>
                    </TextBlock>
                </DockPanel>
            </Grid>
        </DockPanel>
    </ScrollViewer>
</UserControl>
