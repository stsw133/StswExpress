<UserControl x:Class="TestApp.StswSplitButtonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:se="clr-namespace:StswExpress;assembly=StswExpress"
             xmlns:local="clr-namespace:TestApp"
             mc:Ignorable="d" d:Background="White" d:DesignHeight="450" d:DesignWidth="750">
    <d:UserControl.DataContext>
        <local:StswSplitButtonContext/>
    </d:UserControl.DataContext>
    
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <DockPanel>
            <Grid DockPanel.Dock="Top">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>
                <!-- name and description -->
                <StackPanel Grid.Column="0" Margin="5">
                    <Label>
                        <StackPanel>
                            <TextBlock Text="{Binding ThisControlName}" FontSize="20" FontWeight="Bold"/>
                            <TextBlock Margin="0 5 0 0" TextAlignment="Justify" TextWrapping="Wrap">
                                Represents a control that combines the functionality of a regular button with a drop-down button.
                            </TextBlock>
                        </StackPanel>
                    </Label>
                    <!-- IsEnabled -->
                    <se:StswCheckBox IsChecked="{Binding IsEnabled}" Margin="0 3">
                        <Label Content="IsEnabled" FontSize="15" FontWeight="Medium"/>
                    </se:StswCheckBox>
                    <!-- IsReadOnly -->
                    <se:StswCheckBox IsChecked="{Binding IsReadOnly}" Margin="0 3">
                        <Label Content="IsReadOnly" FontSize="15" FontWeight="Medium"/>
                    </se:StswCheckBox>
                </StackPanel>
                <GridSplitter Margin="-5" Width="5"/>
                <!-- control -->
                <DockPanel Grid.Column="1" Margin="5" VerticalAlignment="Top">
                    <se:StswSplitButton DockPanel.Dock="Top"
                                        IsDropDownOpen="{Binding IsDropDownOpen}"
                                        IsEnabled="{Binding IsEnabled}"
                                        IsReadOnly="{Binding IsReadOnly}"
                                        Margin="5"
                                        Command="{Binding OnClickCommand}" CommandParameter="6">
                        <se:StswSplitButton.Header>
                            <se:StswHeader IconData="{x:Static se:StswIcons.Dice6}" Content="Here is a text for the header"/>
                        </se:StswSplitButton.Header>

                        <se:StswButton BorderThickness="0" CornerRadius="0" HorizontalContentAlignment="Left" Command="{Binding OnClickCommand}" CommandParameter="1">
                            <se:StswHeader IconData="{x:Static se:StswIcons.Dice1}" Content="Option 1"/>
                        </se:StswButton>
                        <se:StswButton BorderThickness="0" CornerRadius="0" HorizontalContentAlignment="Left" Command="{Binding OnClickCommand}" CommandParameter="2">
                            <se:StswHeader IconData="{x:Static se:StswIcons.Dice2}" Content="Option 2"/>
                        </se:StswButton>
                        <se:StswButton BorderThickness="0" CornerRadius="0" HorizontalContentAlignment="Left" Command="{Binding OnClickCommand}" CommandParameter="3">
                            <se:StswHeader IconData="{x:Static se:StswIcons.Dice3}" Content="Option 3"/>
                        </se:StswButton>
                        <se:StswButton BorderThickness="0" CornerRadius="0" HorizontalContentAlignment="Left" Command="{Binding OnClickCommand}" CommandParameter="4">
                            <se:StswHeader IconData="{x:Static se:StswIcons.Dice4}" Content="Option 4"/>
                        </se:StswButton>
                        <se:StswButton BorderThickness="0" CornerRadius="0" HorizontalContentAlignment="Left" Command="{Binding OnClickCommand}" CommandParameter="5">
                            <se:StswHeader IconData="{x:Static se:StswIcons.Dice5}" Content="Option 5"/>
                        </se:StswButton>
                    </se:StswSplitButton>
                    <TextBlock DockPanel.Dock="Top" Margin="10">
                        <Run Text="You clicked option "/>
                        <Run Text="{Binding ClickOption}" FontWeight="Bold"/>
                    </TextBlock>
                </DockPanel>
            </Grid>
        </DockPanel>
    </ScrollViewer>
</UserControl>
