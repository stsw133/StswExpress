<UserControl x:Class="TestApp.StswTabControlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:se="clr-namespace:StswExpress;assembly=StswExpress"
             xmlns:local="clr-namespace:TestApp"
             mc:Ignorable="d" d:Background="White" d:DesignHeight="450" d:DesignWidth="750">
    <d:UserControl.DataContext>
        <local:StswTabControlContext/>
    </d:UserControl.DataContext>
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <DockPanel VerticalAlignment="Top">
            <Label DockPanel.Dock="Top" Content="Disabled:"/>
            <se:StswTabControl DockPanel.Dock="Top" Margin="5" AreTabsVisible="True" ItemsSource="{Binding ItemsDisabled}" SelectedIndex="0" IsEnabled="False"/>
            <Label DockPanel.Dock="Top" Content="Enabled:"/>
            <se:StswTabControl DockPanel.Dock="Top" Margin="5" AreTabsVisible="True" ItemsSource="{Binding Items}" SelectedIndex="0">
                <se:StswTabControl.NewTabTemplate>
                    <se:StswTabItemModel Icon="{x:Static se:StswIcons.Glasses}" Name="New tab" Type="{x:Type local:ContractorsContext}"/>
                </se:StswTabControl.NewTabTemplate>
            </se:StswTabControl>
        </DockPanel>
    </ScrollViewer>
</UserControl>
