<UserControl x:Class="TestApp.StswPagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:se="clr-namespace:StswExpress;assembly=StswExpress"
             xmlns:local="clr-namespace:TestApp"
             mc:Ignorable="d" d:Background="White" d:DesignHeight="450" d:DesignWidth="750">
    <d:UserControl.DataContext>
        <local:StswPagerContext/>
    </d:UserControl.DataContext>

    <local:ControlsBase ContentAlignmentVisibility="Visible">
        <local:ControlsBase.Description>
            Represents a control that extends the ScrollViewer class with additional functionality.
        </local:ControlsBase.Description>
        <!-- properties -->
        <local:ControlsBase.Properties>
            <!-- ItemsSource -->
            <se:StswText FontSize="15" FontWeight="Medium" Padding="3" Text="ItemsSource"/>
            <se:StswButton Margin="0 0 0 3" Command="{Binding SelectItemsSourceCommand}" HorizontalContentAlignment="{Binding DirectoryPath, Converter={x:Static se:StswIfElseConverter.Instance}, ConverterParameter='~Center~Left'}">
                <se:StswHeader Content="{Binding DirectoryPath}" IconData="{x:Static se:StswIcons.FolderOpen}" IconFill="{DynamicResource StswColor.Structure}" IconScale="1.33" Padding="0"/>
            </se:StswButton>
            <!-- Orientation -->
            <se:StswText FontSize="15" FontWeight="Medium" Padding="3" Text="Orientation"/>
            <se:StswComboBox ItemsSource="{Binding Converter={x:Static se:StswEnumToListConverter.Instance}, ConverterParameter={x:Type Orientation}}"
                             DisplayMemberPath="Display" Margin="0 0 0 3" SelectedValue="{Binding Orientation}" SelectedValuePath="Value"/>
        </local:ControlsBase.Properties>
        <!-- content -->
        <se:StswPager IsEnabled="{Binding IsEnabled}"
                      ItemsSource="{Binding ItemsSource}"
                      Margin="5"
                      Orientation="{Binding Orientation}"
                      HorizontalAlignment="{Binding HorizontalAlignment}"
                      HorizontalContentAlignment="{Binding HorizontalContentAlignment}"
                      VerticalAlignment="{Binding VerticalAlignment}"
                      VerticalContentAlignment="{Binding VerticalContentAlignment}">
            <se:StswZoomControl>
                <se:StswImage Source="{Binding}"/>
            </se:StswZoomControl>
        </se:StswPager>
        <!-- status panel -->
        
    </local:ControlsBase>
</UserControl>
