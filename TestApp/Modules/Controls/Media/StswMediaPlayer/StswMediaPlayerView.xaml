<UserControl x:Class="TestApp.StswMediaPlayerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:se="clr-namespace:StswExpress;assembly=StswExpress"
             xmlns:local="clr-namespace:TestApp"
             mc:Ignorable="d" d:Background="White" d:DesignHeight="450" d:DesignWidth="750">
    <d:UserControl.DataContext>
        <local:StswMediaPlayerContext/>
    </d:UserControl.DataContext>

    <local:ControlsBase ContentAlignmentVisibility="Visible">
        <local:ControlsBase.Description>
            Represents a control to display media element with additional features such as management panel.
        </local:ControlsBase.Description>
        <!-- properties -->
        <local:ControlsBase.Properties>
            <!-- Source -->
            <se:StswText FontSize="15" FontWeight="Medium" Padding="3" Text="Source"/>
            <se:StswButton Margin="0 0 0 3" Command="{Binding SelectItemSourceCommand}" HorizontalContentAlignment="{Binding FilePath, Converter={x:Static se:StswIfElseConverter.Instance}, ConverterParameter='~Center~Left'}">
                <se:StswHeader Content="{Binding FilePath}" IconData="{x:Static se:StswIcons.FileImport}" IconFill="{DynamicResource StswColor.Neutral}" IconScale="1.33" Padding="0"/>
            </se:StswButton>
        </local:ControlsBase.Properties>
        <!-- content -->
        <!--
        <se:StswMediaPlayer IsEnabled="{Binding IsEnabled}"
                            Source="{Binding Source}"
                            HorizontalAlignment="{Binding HorizontalAlignment}"
                            HorizontalContentAlignment="{Binding HorizontalContentAlignment}"
                            VerticalAlignment="{Binding VerticalAlignment}"
                            VerticalContentAlignment="{Binding VerticalContentAlignment}"/>
        -->
        <!-- status panel -->

    </local:ControlsBase>
</UserControl>
