<UserControl x:Class="TestApp.StswIconView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:se="clr-namespace:StswExpress;assembly=StswExpress"
             xmlns:local="clr-namespace:TestApp"
             mc:Ignorable="d" d:Background="White" d:DesignHeight="450" d:DesignWidth="750">
    <d:UserControl.DataContext>
        <local:StswIconContext/>
    </d:UserControl.DataContext>

    <local:ControlsBase ContentAlignmentVisibility="Collapsed">
        <local:ControlsBase.Description>
            Represents a control displaying vector icon.
        </local:ControlsBase.Description>
        <!-- properties -->
        <local:ControlsBase.Properties>
            <!-- Icons -->
            <se:StswText FontSize="15" FontWeight="Medium" Padding="3" Text="Icons"/>
            <se:StswComboBox CornerClipping="True" DisplayMemberPath="Display" IsEditable="True" ItemsSource="{Binding Icons}" Margin="0 0 0 3" SelectedValue="{Binding Data}" SelectedValuePath="Value"/>
            <!-- Scale -->
            <se:StswText FontSize="15" FontWeight="Medium" Padding="3" Text="Scale"/>
            <se:StswTextBox Margin="0 0 0 3" Text="{Binding Scale}">
                <se:StswTextBox.Components>
                    <se:StswComponentButton Command="{Binding SetGridLengthFillCommand}" IconData="{x:Static se:StswIcons.Star}"/>
                    <se:StswComponentButton Command="{Binding SetGridLengthAutoCommand}" IconData="{x:Static se:StswIcons.AlphaA}" IconScale="2" Margin="-2"/>
                </se:StswTextBox.Components>
            </se:StswTextBox>
        </local:ControlsBase.Properties>
        <!-- content -->
        <se:StswIcon Data="{Binding Data}"
                     IsEnabled="{Binding IsEnabled}"
                     Margin="5"
                     Scale="{Binding Scale}"
                     HorizontalAlignment="{Binding HorizontalAlignment}"
                     VerticalAlignment="{Binding VerticalAlignment}"/>
        <!-- status panel -->
        
    </local:ControlsBase>
</UserControl>
