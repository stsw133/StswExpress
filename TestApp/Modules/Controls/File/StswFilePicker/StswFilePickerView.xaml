<UserControl x:Class="TestApp.StswFilePickerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:se="clr-namespace:StswExpress;assembly=StswExpress"
             xmlns:local="clr-namespace:TestApp"
             mc:Ignorable="d" d:Background="White" d:DesignHeight="450" d:DesignWidth="750">
    <d:UserControl.DataContext>
        <local:StswFilePickerContext/>
    </d:UserControl.DataContext>

    <local:ControlsBase ContentAlignmentVisibility="Visible">
        <local:ControlsBase.Description>
            ...
        </local:ControlsBase.Description>
        <!-- properties -->
        <local:ControlsBase.Properties>
            <!-- Components -->
            <se:StswText FontSize="15" FontWeight="Medium" Padding="3" Text="Components"/>
            <se:StswToggleSwitch HorizontalAlignment="Left" IsChecked="{Binding Components}"/>
            <!-- IsReadOnly -->
            <se:StswText FontSize="15" FontWeight="Medium" Padding="3" Text="IsReadOnly"/>
            <se:StswToggleSwitch HorizontalAlignment="Left" IsChecked="{Binding IsReadOnly}"/>
            <!-- IsShiftingEnabled -->
            <se:StswText FontSize="15" FontWeight="Medium" Padding="3" Text="IsShiftingEnabled"/>
            <se:StswToggleSwitch HorizontalAlignment="Left" IsChecked="{Binding IsShiftingEnabled}"/>
            <!-- PathType -->
            <se:StswText FontSize="15" FontWeight="Medium" Padding="3" Text="PathType"/>
            <se:StswComboBox ItemsSource="{Binding Converter={x:Static se:StswEnumToListConverter.Instance}, ConverterParameter={x:Type se:StswPathType}}"
                             DisplayMemberPath="Display" Margin="0 0 0 3" SelectedValue="{Binding PathType}" SelectedValuePath="Value"/>
        </local:ControlsBase.Properties>
        <!-- content -->
        <se:StswFilePicker IsEnabled="{Binding IsEnabled}"
                           IsReadOnly="{Binding IsReadOnly}"
                           IsShiftingEnabled="{Binding IsShiftingEnabled}"
                           Margin="5"
                           PathType="{Binding PathType}"
                           Placeholder="Select path..."
                           SelectedPath="{Binding SelectedPath}"
                           HorizontalAlignment="{Binding HorizontalAlignment}"
                           HorizontalContentAlignment="{Binding HorizontalContentAlignment}"
                           VerticalAlignment="{Binding VerticalAlignment}"
                           VerticalContentAlignment="{Binding VerticalContentAlignment}">
            <se:StswFilePicker.Components>
                <se:StswComponentButton Command="{Binding ClearCommand}"
                                        IconData="{x:Static se:StswIcons.Close}"
                                        Visibility="{Binding Components, Converter={x:Static se:StswBoolConverter.Instance}}"/>
            </se:StswFilePicker.Components>
        </se:StswFilePicker>
        <!-- status panel -->
        
    </local:ControlsBase>
</UserControl>
