<UserControl x:Class="TestApp.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:se="clr-namespace:StswExpress;assembly=StswExpress"
             xmlns:local="clr-namespace:TestApp"
             mc:Ignorable="d" d:Background="White" d:DesignHeight="450" d:DesignWidth="750">
    <d:UserControl.DataContext>
        <local:HomeContext/>
    </d:UserControl.DataContext>
    <se:StswScrollViewer IsDynamic="True" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
        <DockPanel Margin="5" HorizontalAlignment="Center" VerticalAlignment="Center">
            <se:StswOutlinedText DockPanel.Dock="Top" Fill="#26bfe6" FontSize="60" Stroke="#2673e5" StrokeThickness="6" Text="stsw" HorizontalAlignment="Center"/>
            <UniformGrid Columns="2" Margin="0 10 0 40">
                <se:StswText DockPanel.Dock="Top" Text="Author:"/>
                <se:StswText DockPanel.Dock="Top" FontWeight="Medium" Text="{Binding Author}"/>

                <se:StswText DockPanel.Dock="Top" Text="App name:"/>
                <se:StswText DockPanel.Dock="Top" FontWeight="Medium" Text="{Binding Name}"/>

                <se:StswText DockPanel.Dock="Top" Text="Version:"/>
                <se:StswText DockPanel.Dock="Top" FontWeight="Medium" Text="{Binding Version}"/>
            </UniformGrid>
        </DockPanel>
    </se:StswScrollViewer>
</UserControl>
