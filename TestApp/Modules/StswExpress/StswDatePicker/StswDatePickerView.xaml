<UserControl x:Class="TestApp.StswDatePickerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:se="clr-namespace:StswExpress;assembly=StswExpress"
             xmlns:local="clr-namespace:TestApp"
             mc:Ignorable="d" d:Background="White" d:DesignHeight="450" d:DesignWidth="750">
    <d:UserControl.DataContext>
        <local:StswDatePickerContext/>
    </d:UserControl.DataContext>
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <DockPanel VerticalAlignment="Top">
            <Label DockPanel.Dock="Top" Content="Disabled:"/>
            <UniformGrid DockPanel.Dock="Top" Rows="1">
                <se:StswDatePicker Margin="5" IsEnabled="False" SelectedDate="{Binding Date}"/>
                <se:StswDatePicker Margin="5" IsEnabled="False" Format="yyyy-MM-dd HH:mm:ss"/>
            </UniformGrid>
            <Label DockPanel.Dock="Top" Content="Read only:"/>
            <UniformGrid DockPanel.Dock="Top" Rows="1">
                <se:StswDatePicker Margin="5" IsReadOnly="True" SelectedDate="{Binding Date}"/>
                <se:StswDatePicker Margin="5" IsReadOnly="True" Format="yyyy-MM-dd HH:mm:ss"/>
            </UniformGrid>
            <Label DockPanel.Dock="Top" Content="Enabled:"/>
            <UniformGrid DockPanel.Dock="Top" Rows="1">
                <se:StswDatePicker Margin="5" SelectedDate="{Binding Date}"/>
                <se:StswDatePicker Margin="5" Format="yyyy-MM-dd HH:mm:ss"/>
            </UniformGrid>
        </DockPanel>
    </ScrollViewer>
</UserControl>
