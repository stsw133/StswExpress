<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:se="clr-namespace:StswExpress.Wpf">

	<Style TargetType="{x:Type se:StswInputBoxTest}">
		<Setter Property="Background" Value="{DynamicResource StswBox.Static.Background}"/>
		<Setter Property="BorderBrush" Value="{DynamicResource StswBox.Static.Border}"/>
		<Setter Property="BorderThickness" Value="2"/>
		<Setter Property="CornerClipping" Value="True"/>
		<Setter Property="CornerRadius" Value="6"/>
		<Setter Property="Padding" Value="3"/>
		<Setter Property="HorizontalScrollBarVisibility" Value="Hidden"/>
		<Setter Property="VerticalScrollBarVisibility" Value="Hidden"/>

		<Setter Property="Errors" Value="{Binding (Validation.Errors), RelativeSource={RelativeSource Self}}"/>
		<Setter Property="HasError" Value="{Binding (Validation.HasError), RelativeSource={RelativeSource Self}}"/>
		<Setter Property="Validation.ErrorTemplate" Value="{x:Null}"/>

		<Setter Property="Focusable" Value="True"/>
		<Setter Property="IsTabStop" Value="True"/>
		<Setter Property="SnapsToDevicePixels" Value="False"/>

		<Setter Property="HorizontalAlignment" Value="Stretch"/>
		<Setter Property="VerticalAlignment" Value="Top"/>
		<Setter Property="HorizontalContentAlignment" Value="Stretch"/>
		<Setter Property="VerticalContentAlignment" Value="Center"/>
		<Setter Property="se:StswFocusVisual.Assign" Value="False"/>

		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="{x:Type se:StswInputBoxTest}">
					<se:StswBorder x:Name="OPT_MainBorder"
                                   FlowDirection="LeftToRight"
                                   Background="{TemplateBinding Background}"
                                   BorderBrush="{TemplateBinding BorderBrush}"
                                   BorderThickness="{TemplateBinding BorderThickness}"
                                   CornerClipping="{TemplateBinding CornerClipping}"
                                   CornerRadius="{TemplateBinding CornerRadius}">
						<DockPanel>
							<ContentPresenter x:Name="OPT_Icon"
                                              ContentSource="Icon"
                                              Focusable="False"
                                              IsHitTestVisible="False"/>
							<ItemsControl x:Name="OPT_SubControls"
                                          DockPanel.Dock="Right"
                                          IsTabStop="False"
                                          ItemsSource="{TemplateBinding SubControls}">
								<ItemsControl.ItemsPanel>
									<ItemsPanelTemplate>
										<StackPanel Orientation="Horizontal"/>
									</ItemsPanelTemplate>
								</ItemsControl.ItemsPanel>
							</ItemsControl>
							<se:StswSubError x:Name="OPT_Error"
                                             DockPanel.Dock="Right"
                                             Errors="{TemplateBinding Errors}"
                                             HasError="{TemplateBinding HasError}"/>
							<se:StswScrollView x:Name="PART_ContentHost"
                                               CanContentScroll="True"
                                               DynamicMode="Partial"
                                               Focusable="False"
                                               Padding="{TemplateBinding Padding}"
                                               SuppressScrollBarLengthInMeasure="True"
                                               HorizontalScrollBarVisibility="{TemplateBinding HorizontalScrollBarVisibility}"
                                               VerticalScrollBarVisibility="{TemplateBinding VerticalScrollBarVisibility}"/>
						</DockPanel>
					</se:StswBorder>
					<ControlTemplate.Triggers>
						<Trigger Property="IsMouseOver" Value="True">
							<Setter TargetName="OPT_MainBorder" Property="Background" Value="{DynamicResource StswBox.MouseOver.Background}"/>
							<Setter TargetName="OPT_MainBorder" Property="BorderBrush" Value="{DynamicResource StswBox.MouseOver.Border}"/>
						</Trigger>
						<Trigger Property="IsKeyboardFocusWithin" Value="True">
							<Setter TargetName="OPT_MainBorder" Property="Background" Value="{DynamicResource StswBox.Focused.Background}"/>
							<Setter TargetName="OPT_MainBorder" Property="BorderBrush" Value="{DynamicResource StswBox.Focused.Border}"/>
						</Trigger>
						<Trigger Property="IsReadOnly" Value="True">
							<Setter TargetName="OPT_MainBorder" Property="Background" Value="{DynamicResource StswBox.ReadOnly.Background}"/>
							<Setter Property="TextElement.Foreground" Value="{DynamicResource StswBox.ReadOnly.Foreground}"/>
						</Trigger>
						<Trigger Property="IsEnabled" Value="False">
							<Setter TargetName="OPT_MainBorder" Property="Background" Value="{DynamicResource StswBox.Disabled.Background}"/>
							<Setter TargetName="OPT_MainBorder" Property="BorderBrush" Value="{DynamicResource StswBox.Disabled.Border}"/>
							<Setter Property="TextElement.Foreground" Value="{DynamicResource StswBox.Disabled.Foreground}"/>
						</Trigger>
						<Trigger Property="HasError" Value="True">
							<Setter TargetName="OPT_MainBorder" Property="BorderBrush" Value="{DynamicResource StswBox.Error.Border}"/>
						</Trigger>
					</ControlTemplate.Triggers>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

</ResourceDictionary>
