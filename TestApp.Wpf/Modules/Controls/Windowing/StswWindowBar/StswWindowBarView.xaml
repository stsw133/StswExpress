<UserControl x:Class="TestApp.Wpf.StswWindowBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:se="clr-namespace:StswExpress.Wpf;assembly=StswExpress.Wpf"
             xmlns:local="clr-namespace:TestApp.Wpf"
             mc:Ignorable="d" d:DataContext="{d:DesignInstance local:StswWindowBarContext}">

    <local:ControlsBase>
        <!-- properties -->

        <!-- content -->
        <se:StswWindowBar IsEnabled="{Binding IsEnabled}"
                          HorizontalAlignment="{Binding HorizontalAlignment}"
                          VerticalAlignment="{Binding VerticalAlignment}"/>
        <!-- status panel -->
        
    </local:ControlsBase>
</UserControl>
