<UserControl x:Class="TestApp.Wpf.StswFileDialogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:se="clr-namespace:StswExpress.Wpf;assembly=StswExpress.Wpf"
             xmlns:local="clr-namespace:TestApp.Wpf"
             mc:Ignorable="d" d:Background="White" d:DataContext="{d:DesignInstance local:StswFileDialogContext}">

    <local:ControlsBase>
        <!-- properties -->
        <local:ControlsBase.Properties>
            <!-- CloseOnBackdropClick -->
            <se:StswText FontSize="15" FontWeight="Medium" Padding="3" Text="{se:StswNameOf CloseOnBackdropClick, Type={x:Type se:StswContentDialog}}"/>
            <se:StswToggleSwitch HorizontalAlignment="Left" IsChecked="{Binding CloseOnBackdropClick}"/>
        </local:ControlsBase.Properties>
        <!-- content -->
        <se:StswContentDialog Identifier="StswFileDialogView" CloseOnBackdropClick="{Binding CloseOnBackdropClick}">
            <se:StswButton Command="{Binding OpenFileDialogCommand}" Content="Open file dialog" VerticalAlignment="Top"/>
        </se:StswContentDialog>
        <!-- status panel -->
        <local:ControlsBase.StatusPanel>
            <se:StswText Padding="5">
                <Run Text="Dialog ended with "/>
                <Run Text="{Binding DialogResult}" FontWeight="Bold"/>
                <Run Text=" result"/>
            </se:StswText>
        </local:ControlsBase.StatusPanel>
    </local:ControlsBase>
</UserControl>
