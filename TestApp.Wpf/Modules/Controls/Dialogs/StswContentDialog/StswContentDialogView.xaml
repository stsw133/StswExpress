<UserControl x:Class="TestApp.Wpf.StswContentDialogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:se="clr-namespace:StswExpress.Wpf;assembly=StswExpress.Wpf"
             xmlns:local="clr-namespace:TestApp.Wpf"
             mc:Ignorable="d" d:DataContext="{d:DesignInstance local:StswContentDialogContext}">

    <local:ControlsBase>
        <!-- properties -->
        <local:ControlsBase.Properties>
            <!-- CloseOnBackdropClick -->
            <se:StswText FontSize="15" FontWeight="Medium" Padding="3" Text="{se:StswNameOf CloseOnBackdropClick, Type={x:Type se:StswContentDialog}}"/>
            <se:StswToggleSwitch HorizontalAlignment="Left" IsChecked="{Binding CloseOnBackdropClick}"/>
        </local:ControlsBase.Properties>
        <!-- content -->
        <se:StswContentDialog Identifier="StswContentDialogView" CloseOnBackdropClick="{Binding CloseOnBackdropClick}">
            <se:StswButton Command="{Binding OpenContentDialogCommand}" Content="Open content dialog" VerticalAlignment="Top"/>
        </se:StswContentDialog>
        <!-- status panel -->

    </local:ControlsBase>
</UserControl>
