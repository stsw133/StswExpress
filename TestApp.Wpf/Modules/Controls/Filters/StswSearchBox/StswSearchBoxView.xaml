<UserControl x:Class="TestApp.Wpf.StswSearchBoxView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:se="clr-namespace:StswExpress.Wpf;assembly=StswExpress.Wpf"
             xmlns:local="clr-namespace:TestApp.Wpf"
             mc:Ignorable="d" d:DataContext="{d:DesignInstance local:StswSearchBoxContext}">

    <local:ControlsBase>
        <!-- properties -->
        <local:ControlsBase.Properties>
            <!-- IsReadOnly -->
            <se:StswText FontSize="15" FontWeight="Medium" Padding="3" Text="{se:StswNameOf IsReadOnly, Type={x:Type se:StswSearchBox}}"/>
            <se:StswToggleSwitch HorizontalAlignment="Left" IsChecked="{Binding IsReadOnly}"/>
            <!-- SubControls -->
            <se:StswText FontSize="15" FontWeight="Medium" Padding="3" Text="{se:StswNameOf SubControls, Type={x:Type se:StswSearchBox}}"/>
            <se:StswToggleSwitch HorizontalAlignment="Left" IsChecked="{Binding SubControls}"/>
        </local:ControlsBase.Properties>
        <!-- content -->
        <se:StswSearchBox IsEnabled="{Binding IsEnabled}"
                          IsReadOnly="{Binding IsReadOnly}"
                          ItemsSource="{Binding Items}"
                          Placeholder="Search for item..."
                          Text="{Binding Text}"
                          HorizontalAlignment="{Binding HorizontalAlignment}"
                          HorizontalContentAlignment="{Binding HorizontalContentAlignment}"
                          VerticalAlignment="{Binding VerticalAlignment}"
                          VerticalContentAlignment="{Binding VerticalContentAlignment}">
            <se:StswSearchBox.SubControls>
                <se:StswSubButton Command="{Binding RandomizeCommand}" IconData="{x:Static se:StswIcons.DiceMultiple}" Visibility="{Binding SubControls}"/>
                <se:StswSubButton Command="{x:Static se:StswCommands.ClearText}" IconData="{x:Static se:StswIcons.Close}" Visibility="{Binding SubControls}"/>
            </se:StswSearchBox.SubControls>
        </se:StswSearchBox>
        <!-- status panel -->
        <local:ControlsBase.StatusPanel>
            <!--
            <se:StswText Padding="5">
                <Run Text="{Binding CriteriaCount}" FontWeight="Bold"/>
                <Run Text=" items match the search criteria"/>
            </se:StswText>
            -->
            <se:StswListBox ItemsSource="{Binding Items}" Height="50"/>
        </local:ControlsBase.StatusPanel>
    </local:ControlsBase>
</UserControl>
